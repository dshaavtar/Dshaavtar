import { _ as useBackendActor, r as reactExports, bM as useAddCity, bN as useUpdateCity, bO as useDeleteCity, bP as useSaveCityControl, bQ as useCreateRestockOrderAdmin, bR as useUpdateMarketplaceItemAdmin, bS as useCreateMarketplaceItem, bT as useDeleteMarketplaceItemAdmin, bU as useDeleteRestockOrderAdmin, bV as useUpdateRestockStatus, bW as useCreateModerationItemAdmin, bX as useUpdateModerationItemStatusAdmin, bY as useDeleteModerationItemAdmin, bZ as useUpdateSupportTicketStatus, b_ as useDeleteSupportTicketAdmin, b$ as useCreateRecipe, c0 as useDeleteRecipe, c1 as useCreateAdhocJobAdmin, c2 as useUpdateAdhocJobStatusAdmin, c3 as useDeleteAdhocJobAdmin, j as jsxRuntimeExports, p as ue } from "./index-D4mmtgjo.js";
import { B as Badge } from "./badge-BlQlNngM.js";
import { B as Button } from "./button-CTnHNrH8.js";
import { I as Input } from "./input-BAihtL8f.js";
import { L as Label } from "./label-Cgfk62Wh.js";
import { S as ScrollArea } from "./scroll-area-C_Pp8Hph.js";
import { S as Skeleton } from "./skeleton-Cwq6arIw.js";
import { S as Switch } from "./switch-CSGIBB-2.js";
import { T as Tabs, a as TabsList, b as TabsTrigger, c as TabsContent } from "./tabs-bpKGDaaq.js";
import { u as useLanguageLearning } from "./useLanguageLearning-CqRAiv3Y.js";
import { s as safeCount } from "./utils-BlceAbYT.js";
import { U as Users } from "./users-BCFHEKUR.js";
import { B as Building2 } from "./building-2-B0h7D8pK.js";
import { P as Package } from "./package-CosknzeL.js";
import { S as ShoppingCart } from "./shopping-cart-CIiL3ef_.js";
import { M as Megaphone } from "./megaphone-BO0GtYln.js";
import { C as Calendar } from "./calendar-DOvJee1H.js";
import { U as UsersRound } from "./users-round-CIqcKsT9.js";
import { B as Briefcase } from "./briefcase-CIHVnHgq.js";
import { H as House } from "./house-DQF9lC4w.js";
import { T as Truck } from "./truck-MwLrSz0P.js";
import { Z as Zap } from "./zap-C7-axDdv.js";
import { c as createLucideIcon } from "./createLucideIcon-BGWdtUCJ.js";
import { M as MessageSquare } from "./message-square-DPd9AoY2.js";
import { S as Send } from "./send-DoOOMmv0.js";
import { G as Globe } from "./globe--tJa3NSQ.js";
import { S as Store } from "./store-CaCzKt5a.js";
import { C as ChefHat } from "./chef-hat-VsY0dCz0.js";
import { C as Clock } from "./clock-CO75OdmO.js";
import { H as Heart } from "./heart-BUx2l_GB.js";
import { M as MapPin } from "./map-pin-DGvTRx32.js";
import { G as Gift } from "./gift-6qBg8KRw.js";
import { T as TrendingUp } from "./trending-up-Zp9L4lXd.js";
import { C as Car } from "./car-C3Ivr9eE.js";
import { D as DoorOpen, U as UtensilsCrossed } from "./utensils-crossed-DJHwAEoW.js";
import { W as Wrench } from "./wrench-B6FcmCok.js";
import { B as BookOpen } from "./book-open-DS2-X7o9.js";
import { C as CircleCheckBig } from "./circle-check-big-C6-kT1pJ.js";
import { F as Flame } from "./flame-34E6_5Fg.js";
import { D as Database } from "./database-CADlqd_q.js";
import { P as Plus } from "./plus-ty49Yili.js";
import { S as Search } from "./search-DnFDW7fF.js";
import { R as RefreshCw } from "./refresh-cw-D1Rv7uio.js";
import { C as ClipboardCopy } from "./clipboard-copy-C_TqIbk0.js";
import { P as Pen } from "./pen-DGASRaG7.js";
import { E as ExternalLink } from "./external-link-BziLgQmT.js";
import { T as Trash2 } from "./trash-2-anyWEWQc.js";
import { X } from "./x-Chksmd6i.js";
import "./index-DPbSRAbD.js";
import "./utils-2v2HxlWs.js";
import "./index-BtrS4JsN.js";
import "./index-CUcO6jhF.js";
import "./index-BNXq-E6T.js";
import "./index-dLX_aGK4.js";
import "./index-yUS8KoxU.js";
import "./index-IXOTxK3N.js";
import "./index-z5OST4d2.js";
import "./index-DYndF6Sn.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  ["path", { d: "M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z", key: "i9b6wo" }],
  ["line", { x1: "4", x2: "4", y1: "22", y2: "15", key: "1cm3nv" }]
];
const Flag = createLucideIcon("flag", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  ["path", { d: "m16 6 4 14", key: "ji33uf" }],
  ["path", { d: "M12 6v14", key: "1n7gus" }],
  ["path", { d: "M8 8v12", key: "1gg7y9" }],
  ["path", { d: "M4 4v16", key: "6qkkli" }]
];
const Library = createLucideIcon("library", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
  ["path", { d: "m4.93 4.93 4.24 4.24", key: "1ymg45" }],
  ["path", { d: "m14.83 9.17 4.24-4.24", key: "1cb5xl" }],
  ["path", { d: "m14.83 14.83 4.24 4.24", key: "q42g0n" }],
  ["path", { d: "m9.17 14.83-4.24 4.24", key: "bqpfvv" }],
  ["circle", { cx: "12", cy: "12", r: "4", key: "4exip2" }]
];
const LifeBuoy = createLucideIcon("life-buoy", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  ["circle", { cx: "6", cy: "19", r: "3", key: "1kj8tv" }],
  ["path", { d: "M9 19h8.5a3.5 3.5 0 0 0 0-7h-11a3.5 3.5 0 0 1 0-7H15", key: "1d8sl" }],
  ["circle", { cx: "18", cy: "5", r: "3", key: "gq8acd" }]
];
const Route = createLucideIcon("route", __iconNode);
const SOURCE_STYLE = {
  whatsapp: {
    label: "WhatsApp",
    cls: "bg-green-100 text-green-700 border-green-200"
  },
  telegram: {
    label: "Telegram",
    cls: "bg-blue-100 text-blue-700 border-blue-200"
  },
  sms: { label: "SMS", cls: "bg-amber-100 text-amber-700 border-amber-200" },
  simulator: {
    label: "Simulator",
    cls: "bg-violet-100 text-violet-700 border-violet-200"
  },
  dashboard: {
    label: "Dashboard",
    cls: "bg-muted text-muted-foreground border-border"
  },
  "admin sample": {
    label: "Admin Sample",
    cls: "bg-orange-100 text-orange-700 border-orange-200"
  }
};
const ALL_SOURCES = [
  "All Sources",
  "WhatsApp",
  "Telegram",
  "SMS",
  "Simulator",
  "Dashboard"
];
function SourceBadge({ source }) {
  const key = (source ?? "").toLowerCase();
  const cfg = SOURCE_STYLE[key];
  if (!cfg)
    return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground text-[10px]", children: source || "—" });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "span",
    {
      className: `inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-medium border ${cfg.cls}`,
      children: cfg.label
    }
  );
}
function sid(v, len = 12) {
  const s = String(v ?? "");
  return s.length > len ? `${s.slice(0, len)}…` : s;
}
const ENTITIES = [
  {
    key: "users",
    label: "Users",
    icon: Users,
    columns: ["id", "phone", "name", "role", "createdAt"],
    fetchFn: (a) => a.listUsers(null),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        phone: row.phone,
        name: row.name,
        role: String(row.role ?? ""),
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—",
        source: String(row.source ?? row.registeredVia ?? "")
      };
    }
  },
  {
    key: "merchants",
    label: "Merchants",
    icon: Building2,
    columns: ["id", "phone", "businessName", "category", "status", "verified"],
    fetchFn: (a) => a.listMerchants(null, null),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        phone: row.ownerPhone,
        businessName: row.businessName,
        category: row.category,
        status: row.isActive ? "Active" : "Inactive",
        verified: row.isVerified ? "✅ Yes" : "⏳ Pending"
      };
    }
  },
  {
    key: "products",
    label: "Products",
    icon: Package,
    columns: ["id", "title", "merchantId", "baseRate", "discount", "condition"],
    fetchFn: async (a) => {
      const merchants = await a.listMerchants(null, null);
      const all = [];
      for (const m of merchants.slice(0, 50)) {
        try {
          const prods = await a.getProductsByMerchant(m.id);
          all.push(...prods);
        } catch {
        }
      }
      return all;
    },
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        title: row.title,
        merchantId: sid(row.merchantId, 10),
        baseRate: `₹${safeCount(row.baseRate)}`,
        discount: `${safeCount(row.specialDiscount)}%`,
        condition: row.isNew ? "New" : "Refurb"
      };
    }
  },
  {
    key: "orders",
    label: "Orders",
    icon: ShoppingCart,
    columns: ["id", "customerId", "merchantId", "status", "total", "createdAt"],
    fetchFn: (a) => a.getAllOrders(
      null,
      null,
      null
    ),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        customerId: sid(row.customerId, 10),
        merchantId: sid(row.merchantId, 10),
        status: String(row.status ?? ""),
        total: `₹${safeCount(row.totalAmount ?? row.total)}`,
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—",
        source: String(row.source ?? row.channel ?? row.placedVia ?? "")
      };
    }
  },
  {
    key: "promotions",
    label: "Promotions",
    icon: Megaphone,
    columns: ["id", "title", "advertiser", "city", "reach", "status"],
    fetchFn: (a) => a.getAllPromotions(),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        title: row.title,
        advertiser: row.advertiserPhone,
        city: row.locationCity,
        reach: `${safeCount(row.reachedCount)}/${safeCount(row.targetUserCount)}`,
        status: String(row.status ?? "")
      };
    }
  },
  {
    key: "events",
    label: "Events",
    icon: Calendar,
    columns: ["id", "name", "organizer", "location", "paid", "status"],
    fetchFn: (a) => a.getAllEvents(),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        name: row.eventName,
        organizer: row.organizerPhone,
        location: row.locationAddress,
        paid: row.isPaid ? `₹${row.price}` : "Free",
        status: String(row.status ?? "")
      };
    }
  },
  {
    key: "familyGroups",
    label: "Family Groups",
    icon: UsersRound,
    columns: ["id", "owner", "relation", "phone", "relationship", "status"],
    fetchFn: (a) => a.getAllFamilyMembers(),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id, 10),
        owner: `${row.ownerName} ${row.ownerSurname ?? ""}`,
        relation: row.relationName,
        phone: row.relationPhone,
        relationship: String(row.relationship ?? ""),
        status: String(row.inviteStatus ?? "")
      };
    }
  },
  {
    key: "jobs",
    label: "Jobs",
    icon: Briefcase,
    columns: ["id", "title", "category", "salary", "location", "open"],
    fetchFn: (a) => a.getAllJobs(null),
    mapRow: (r) => {
      var _a;
      const row = r;
      return {
        id: sid(row.id),
        title: row.title,
        category: row.category,
        salary: `₹${row.salaryMin ?? 0}–₹${row.salaryMax ?? 0}`,
        location: ((_a = row.location) == null ? void 0 : _a.address) ?? "—",
        open: row.isOpen ? "Open" : "Closed"
      };
    }
  },
  {
    key: "properties",
    label: "Properties",
    icon: House,
    columns: ["id", "type", "price", "location", "posterId", "active"],
    fetchFn: (a) => a.getAllProperties(
      null,
      null
    ),
    mapRow: (r) => {
      var _a;
      const row = r;
      return {
        id: sid(row.id),
        type: String(row.listingType ?? ""),
        price: `₹${row.expectedPrice ?? 0}`,
        location: ((_a = row.location) == null ? void 0 : _a.address) ?? "—",
        posterId: sid(row.posterId, 10),
        active: row.isActive ? "Active" : "Inactive"
      };
    }
  },
  {
    key: "deliveryPartners",
    label: "Delivery Partners",
    icon: Truck,
    columns: [
      "id",
      "phone",
      "name",
      "vehicle",
      "service",
      "verified",
      "online"
    ],
    fetchFn: (a) => a.listAllDeliveryPartners(),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        phone: row.phone ?? row.ownerPhone ?? row.contactPhone ?? "—",
        name: row.name,
        vehicle: String(row.vehicleType ?? ""),
        service: String(row.serviceType ?? ""),
        verified: row.isVerified ? "✅" : "⏳",
        online: row.isOnline ? "🟢 Online" : "⚫ Offline"
      };
    }
  },
  {
    key: "sarthiPartners",
    label: "Sarthi Partners",
    icon: Zap,
    columns: ["id", "phone", "name", "vehicle", "verified", "online"],
    fetchFn: async (a) => {
      const all = await a.listAllDeliveryPartners();
      return all.filter((p) => p.serviceType === "sarthi");
    },
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        phone: row.phone ?? row.ownerPhone ?? row.contactPhone ?? "—",
        name: row.name,
        vehicle: String(row.vehicleType ?? ""),
        verified: row.isVerified ? "✅ Yes" : "⏳ Pending",
        online: row.isOnline ? "🟢 Online" : "⚫ Offline"
      };
    }
  },
  {
    key: "subscriptions",
    label: "Subscriptions",
    icon: Zap,
    columns: ["id", "name", "targetRole", "price", "duration", "active"],
    fetchFn: (a) => a.getAllPlans(null),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        name: row.name,
        targetRole: String(row.targetRole ?? ""),
        price: `₹${row.priceFlat ?? 0}`,
        duration: `${row.durationDays ?? 0}d`,
        active: row.isActive !== false ? "Active" : "Inactive"
      };
    }
  },
  {
    key: "rateCards",
    label: "Rate Cards",
    icon: Truck,
    columns: ["id", "vehicle", "service", "baseRate", "perKm", "active"],
    fetchFn: (a) => a.getAllRateCards(),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        vehicle: String(row.vehicleType ?? ""),
        service: String(row.serviceType ?? ""),
        baseRate: `₹${row.baseRate ?? 0}`,
        perKm: `₹${row.perKmRate ?? 0}/km`,
        active: row.isActive ? "Active" : "Inactive"
      };
    }
  },
  {
    key: "shuttleRoutes",
    label: "Shuttle Routes",
    icon: Route,
    columns: ["id", "route", "source", "dest", "fare", "seats"],
    fetchFn: (a) => a.listShuttleRoutes(),
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        route: row.routeName,
        source: row.source,
        dest: row.destination,
        fare: `₹${safeCount(row.fare)}`,
        seats: String(safeCount(row.availableSeats))
      };
    }
  },
  {
    key: "whatsappConfig",
    label: "WhatsApp Config",
    icon: MessageSquare,
    columns: ["key", "value", "category"],
    fetchFn: async (a) => {
      try {
        const fn = a.getWhatsAppConfigTable;
        if (typeof fn === "function") {
          const rows = await fn();
          if (Array.isArray(rows) && rows.length > 0) return rows;
        }
      } catch {
      }
      try {
        const fn = a.getWhatsAppConfig;
        if (typeof fn === "function") {
          const raw = await fn();
          if (raw && typeof raw === "object" && !("__kind__" in raw)) {
            const c = raw;
            return [
              {
                key: "webhookUrl",
                value: String(c.webhookUrl ?? "—"),
                category: "Webhook"
              },
              {
                key: "phoneNumberId",
                value: String(c.phoneNumberId ?? "—"),
                category: "Credentials"
              },
              {
                key: "verifyToken",
                value: c.verifyToken ? "***hidden***" : "—",
                category: "Security"
              },
              {
                key: "isTestMode",
                value: c.isTestMode ? "Test Mode" : "Live Mode",
                category: "Config"
              },
              {
                key: "isConnected",
                value: c.isConnected ? "Connected" : "Disconnected",
                category: "Status"
              },
              {
                key: "businessAccountId",
                value: String(c.businessAccountId ?? "—"),
                category: "Credentials"
              }
            ];
          }
        }
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        key: row.key,
        value: row.value ? String(row.value).slice(0, 30) + (String(row.value).length > 30 ? "…" : "") : "—",
        category: row.category ?? "—"
      };
    }
  },
  {
    key: "telegramConfig",
    label: "Telegram Config",
    icon: Send,
    columns: ["key", "value", "category"],
    fetchFn: async (a) => {
      try {
        const fn = a.getTelegramConfigTable;
        if (typeof fn === "function") {
          const rows = await fn();
          if (Array.isArray(rows) && rows.length > 0) return rows;
        }
      } catch {
      }
      try {
        const fn = a.getTelegramConfig;
        if (typeof fn === "function") {
          const cfg = await fn();
          if (cfg && typeof cfg === "object" && !("__kind__" in cfg)) {
            const c = cfg;
            return [
              {
                key: "botUsername",
                value: String(c.botUsername ?? "—"),
                category: "Identity"
              },
              {
                key: "alertChatId",
                value: String(c.alertChatId ?? "—"),
                category: "Notifications"
              },
              {
                key: "isEnabled",
                value: c.isEnabled ? "Enabled" : "Disabled",
                category: "Config"
              },
              {
                key: "isConnected",
                value: c.isConnected ? "Connected" : "Disconnected",
                category: "Status"
              },
              {
                key: "botToken",
                value: c.botToken ? "***hidden***" : "—",
                category: "Security"
              }
            ];
          }
        }
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        key: row.key ?? "—",
        value: String(row.value ?? "—").slice(0, 30) + (String(row.value ?? "").length > 30 ? "…" : ""),
        category: row.category ?? "Config"
      };
    }
  },
  {
    key: "ondcEnrollments",
    label: "ONDC Enrollments",
    icon: Globe,
    columns: ["id", "businessName", "role", "gstin", "status", "submittedAt"],
    fetchFn: async (a) => {
      try {
        const fn = a.getOndcEnrollments;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        id: sid(row.id),
        businessName: row.businessName ?? "—",
        role: String(row.role ?? ""),
        gstin: row.gstin ?? "—",
        status: String(row.enrollmentStatus ?? ""),
        submittedAt: row.submittedAt ? new Date(Number(row.submittedAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  {
    key: "ondcSetupConfig",
    label: "ONDC Setup Config",
    icon: Globe,
    columns: ["key", "value", "category"],
    fetchFn: async (a) => {
      try {
        const fn = a.getOndcConfigTable;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [
        { key: "network", value: "ONDC", category: "Setup" },
        { key: "version", value: "2.0", category: "Setup" },
        { key: "environment", value: "staging", category: "Setup" }
      ];
    },
    mapRow: (r) => {
      const row = r;
      return {
        key: row.key ?? "—",
        value: String(row.value ?? "—").slice(0, 40),
        category: row.category ?? "Config"
      };
    }
  },
  // ── Cities ────────────────────────────────────────────────────────────────
  {
    key: "cities",
    label: "Cities",
    icon: Building2,
    idField: "id",
    canAdd: true,
    canEdit: true,
    canDelete: true,
    columns: ["cityName", "pincode", "status", "createdAt"],
    fetchFn: async (a) => {
      try {
        const fn = a.listCities;
        if (typeof fn === "function") {
          const result = await fn();
          if (Array.isArray(result)) return result;
        }
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        cityName: row.cityName ?? row.name ?? "—",
        pincode: row.pincode ?? "—",
        status: row.isEnabled ? "Active" : "Disabled",
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── City Controls ─────────────────────────────────────────────────────────
  {
    key: "cityControls",
    label: "City Controls",
    icon: Building2,
    canAdd: true,
    columns: ["cityName", "moduleName", "enabled"],
    fetchFn: async (a) => {
      try {
        const fn = a.listCityControls;
        if (typeof fn === "function") return fn();
      } catch {
      }
      try {
        const fn = a.getCityControls;
        if (typeof fn === "function") return fn();
      } catch {
      }
      try {
        const fn = a.getCityControlsForDataExplorer;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.cityId ?? row.id ?? "",
        _raw: row,
        cityName: row.cityName ?? "—",
        moduleName: row.moduleName ?? "—",
        enabled: row.enabled ? "Active" : "Disabled"
      };
    }
  },
  // ── Restock Orders ────────────────────────────────────────────────────────
  {
    key: "restockOrders",
    label: "Restock Orders",
    icon: Package,
    idField: "id",
    canAdd: true,
    canDelete: true,
    canStatusUpdate: true,
    statusOptions: ["pending", "accepted", "delivered", "cancelled"],
    columns: [
      "id",
      "merchantId",
      "supplierName",
      "items",
      "status",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.listRestockOrders;
        if (typeof fn === "function") return fn(null);
      } catch {
      }
      try {
        const fn = a.getAllRestockOrders;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      const items = Array.isArray(row.items) ? `${row.items.length} item(s)` : String(row.items ?? "—");
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "pending"),
        _raw: row,
        id: sid(row.id),
        merchantId: sid(row.merchantId, 10),
        supplierName: row.supplierName ?? "—",
        items,
        status: String(row.status ?? "—"),
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—",
        source: String(row.source ?? row.channel ?? "")
      };
    }
  },
  // ── Marketplace Items ─────────────────────────────────────────────────────
  {
    key: "marketplaceItems",
    label: "Marketplace Items",
    icon: Store,
    idField: "id",
    canAdd: true,
    canEdit: true,
    canDelete: true,
    columns: [
      "title",
      "price",
      "category",
      "rentOrSale",
      "active",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.listMarketplaceItems;
        if (typeof fn === "function") return fn(null, null);
      } catch {
      }
      try {
        const fn = a.getAllMarketplaceItems;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        title: row.title ?? "—",
        price: `₹${safeCount(row.price)}`,
        category: row.category ?? "—",
        rentOrSale: String(row.rentOrSale ?? row.listingType ?? "—"),
        active: row.isActive ? "Active" : "Disabled",
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—",
        source: String(row.source ?? row.channel ?? row.listedVia ?? "")
      };
    }
  },
  // ── Moderation Queue ──────────────────────────────────────────────────────
  {
    key: "moderationQueue",
    label: "Moderation Queue",
    icon: Flag,
    idField: "id",
    canAdd: true,
    canDelete: true,
    canStatusUpdate: true,
    statusOptions: ["pending", "approved", "rejected", "flagged"],
    columns: [
      "contentType",
      "contentId",
      "flagReason",
      "status",
      "remarks",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.listModerationItems;
        if (typeof fn === "function") return fn(null);
      } catch {
      }
      try {
        const fn = a.getAllModerationItems;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "pending"),
        _raw: row,
        contentType: String(row.contentType ?? "—"),
        contentId: sid(row.contentId),
        flagReason: String(row.flagReason ?? row.reason ?? "—"),
        status: String(row.status ?? "—"),
        remarks: String(row.remarks ?? "—"),
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── Support Tickets ───────────────────────────────────────────────────────
  {
    key: "supportTickets",
    label: "Support Tickets",
    icon: LifeBuoy,
    idField: "id",
    canDelete: true,
    canStatusUpdate: true,
    statusOptions: ["open", "in-progress", "resolved", "closed"],
    columns: [
      "id",
      "creatorRole",
      "issueType",
      "orderId",
      "status",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllSupportTicketsFull;
        if (typeof fn === "function") return fn(null);
      } catch {
      }
      try {
        const fn = a.listSupportTickets;
        if (typeof fn === "function") return fn(null);
      } catch {
      }
      try {
        const fn = a.getAllSupportTickets;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "open"),
        _raw: row,
        id: sid(row.id),
        creatorRole: String(row.creatorRole ?? row.role ?? "—"),
        issueType: String(row.issueType ?? row.type ?? "—"),
        orderId: row.orderId ? sid(row.orderId) : "—",
        status: String(row.status ?? "—"),
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—",
        source: String(row.source ?? row.channel ?? row.raisedVia ?? "")
      };
    }
  },
  // ── Recipes ───────────────────────────────────────────────────────────────
  {
    key: "recipes",
    label: "Recipes",
    icon: ChefHat,
    idField: "id",
    canAdd: true,
    canDelete: true,
    columns: ["title", "userId", "ingredientCount", "rating", "createdAt"],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllRecipes;
        if (typeof fn === "function") return fn();
      } catch {
      }
      try {
        const fn = a.getAllRecipesTable;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      const ingredients = Array.isArray(row.ingredients) ? row.ingredients.length : safeCount(row.ingredientCount);
      return {
        _id: row.id ?? "",
        _raw: row,
        title: row.title ?? "—",
        userId: sid(row.userId ?? row.authorId, 10),
        ingredientCount: String(ingredients),
        rating: row.rating ? `${Number(row.rating).toFixed(1)} ★` : "—",
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── Adhoc Jobs ────────────────────────────────────────────────────────────
  {
    key: "adhocJobs",
    label: "Adhoc Jobs",
    icon: Clock,
    idField: "id",
    canAdd: true,
    canDelete: true,
    canStatusUpdate: true,
    statusOptions: ["open", "in-progress", "completed", "cancelled"],
    columns: ["title", "category", "price", "jobType", "active", "createdAt"],
    fetchFn: async (a) => {
      try {
        const fn = a.listAllAdhocJobs;
        if (typeof fn === "function") return fn();
      } catch {
      }
      try {
        const fn = a.listAdhocJobsAdmin;
        if (typeof fn === "function") return fn(null);
      } catch {
      }
      try {
        const fn = a.getAllAdhocJobs;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "open"),
        _raw: row,
        title: row.title ?? "—",
        category: row.category ?? "—",
        price: `₹${safeCount(row.price)}`,
        jobType: String(row.jobType ?? row.type ?? "—"),
        active: row.isActive ? "Active" : "Disabled",
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—",
        source: String(row.source ?? row.channel ?? row.postedVia ?? "")
      };
    }
  },
  // ── Healthcare Providers ──────────────────────────────────────────────────────
  {
    key: "healthcareProviders",
    label: "Healthcare Providers",
    icon: Users,
    idField: "id",
    canAdd: true,
    canEdit: true,
    canDelete: true,
    columns: [
      "ID",
      "Name",
      "Specialization",
      "Location",
      "Phone",
      "Available Days",
      "Fee",
      "Created"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllHealthcareProviders;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        ID: sid(row.id),
        Name: row.name ?? "—",
        Specialization: row.specialization ?? "—",
        Location: row.location ?? "—",
        Phone: row.phone ?? "—",
        "Available Days": row.availableDays ?? "—",
        Fee: row.fee !== void 0 ? `₹${safeCount(row.fee)}` : "—",
        Created: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── Tour Operators ────────────────────────────────────────────────────────────
  {
    key: "tourOperators",
    label: "Tour Operators",
    icon: Globe,
    idField: "id",
    canAdd: true,
    canEdit: true,
    canDelete: true,
    columns: [
      "ID",
      "Name",
      "Destination",
      "Type",
      "Duration",
      "Price",
      "Phone",
      "Created"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllTourOperators;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        ID: sid(row.id),
        Name: row.name ?? "—",
        Destination: row.destination ?? "—",
        Type: row.tourType ?? "—",
        Duration: row.duration ?? "—",
        Price: row.price !== void 0 ? `₹${safeCount(row.price)}` : "—",
        Phone: row.phone ?? "—",
        Created: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── Professional Services ─────────────────────────────────────────────────────
  {
    key: "professionalServices",
    label: "Professional Services",
    icon: Briefcase,
    idField: "id",
    canAdd: true,
    canEdit: true,
    canDelete: true,
    columns: [
      "ID",
      "Name",
      "Service Type",
      "Location",
      "Phone",
      "Experience",
      "Hourly Rate",
      "Created"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllProfessionalServices;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        ID: sid(row.id),
        Name: row.name ?? "—",
        "Service Type": row.serviceType ?? "—",
        Location: row.location ?? "—",
        Phone: row.phone ?? "—",
        Experience: row.experience ?? "—",
        "Hourly Rate": row.hourlyRate !== void 0 ? `₹${safeCount(row.hourlyRate)}/hr` : "—",
        Created: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── Healthcare Appointments ───────────────────────────────────────────────────
  {
    key: "healthcareAppointments",
    label: "Healthcare Appointments",
    icon: Heart,
    idField: "id",
    canDelete: true,
    columns: [
      "id",
      "providerId",
      "customerPhone",
      "date",
      "timeSlot",
      "status",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getHealthcareAppointments;
        if (typeof fn === "function") return fn(null, null);
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? ""),
        _raw: row,
        id: sid(row.id),
        providerId: sid(row.providerId, 10),
        customerPhone: row.customerPhone ?? "—",
        date: row.date ?? "—",
        timeSlot: row.timeSlot ?? "—",
        status: String(row.status ?? "—"),
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── Tour Bookings ─────────────────────────────────────────────────────────────
  {
    key: "tourBookings",
    label: "Tour Bookings",
    icon: MapPin,
    idField: "id",
    canDelete: true,
    columns: [
      "id",
      "operatorId",
      "customerPhone",
      "destination",
      "passengerCount",
      "date",
      "status",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getTourBookings;
        if (typeof fn === "function") return fn(null, null);
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? ""),
        _raw: row,
        id: sid(row.id),
        operatorId: sid(row.operatorId, 10),
        customerPhone: row.customerPhone ?? "—",
        destination: row.destination ?? "—",
        passengerCount: String(Number(row.passengerCount ?? 0)),
        date: row.date ?? "—",
        status: String(row.status ?? "—"),
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── Service Bookings ──────────────────────────────────────────────────────────
  {
    key: "serviceBookings",
    label: "Service Bookings",
    icon: Briefcase,
    idField: "id",
    canDelete: true,
    columns: [
      "id",
      "serviceId",
      "customerPhone",
      "date",
      "timeSlot",
      "duration",
      "status",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getServiceBookings;
        if (typeof fn === "function") return fn(null, null);
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? ""),
        _raw: row,
        id: sid(row.id),
        serviceId: sid(row.serviceId, 10),
        customerPhone: row.customerPhone ?? "—",
        date: row.date ?? "—",
        timeSlot: row.timeSlot ?? "—",
        duration: `${Number(row.duration ?? 0)}h`,
        status: String(row.status ?? "—"),
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── Donations ─────────────────────────────────────────────────────────────────
  {
    key: "donations",
    label: "Donations",
    icon: Gift,
    idField: "id",
    canAdd: true,
    canDelete: true,
    canStatusUpdate: true,
    statusOptions: ["Available", "Claimed", "Removed"],
    columns: [
      "id",
      "category",
      "description",
      "quantity",
      "location",
      "donorName",
      "donorPhone",
      "status",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getDonations;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "Available"),
        _raw: row,
        id: sid(row.id),
        category: row.category ?? "—",
        description: String(row.description ?? "—").slice(0, 30),
        quantity: String(row.quantity ?? "—"),
        location: row.location ?? "—",
        donorName: row.donorName ?? "—",
        donorPhone: row.donorPhone ?? row.contactPhone ?? "—",
        status: String(row.status ?? "—"),
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—",
        source: String(row.source ?? "")
      };
    }
  },
  // ── Donation Requests ─────────────────────────────────────────────────────────
  {
    key: "donationRequests",
    label: "Donation Requests",
    icon: Gift,
    idField: "id",
    canAdd: true,
    canDelete: true,
    canStatusUpdate: true,
    statusOptions: ["Pending", "Fulfilled", "Cancelled"],
    columns: [
      "id",
      "category",
      "description",
      "quantityNeeded",
      "location",
      "requesterName",
      "requesterPhone",
      "status",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getDonationRequests;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "Pending"),
        _raw: row,
        id: sid(row.id),
        category: row.category ?? "—",
        description: String(row.description ?? "—").slice(0, 30),
        quantityNeeded: String(row.quantityNeeded ?? "—"),
        location: row.location ?? "—",
        requesterName: row.requesterName ?? "—",
        requesterPhone: row.requesterPhone ?? "—",
        status: String(row.status ?? "—"),
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—",
        source: String(row.source ?? "")
      };
    }
  },
  // ── Election Results ──────────────────────────────────────────────────────
  {
    key: "electionResults",
    label: "Election Results",
    icon: Flag,
    idField: "id",
    canDelete: false,
    columns: [
      "id",
      "state",
      "constituency",
      "partyName",
      "candidateName",
      "votes",
      "voteShare",
      "status"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllElectionResults;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        state: row.state ?? "—",
        constituency: row.constituency ?? "—",
        partyName: row.partyName ?? "—",
        candidateName: row.candidateName ?? "—",
        votes: Number(row.votes ?? 0).toLocaleString("en-IN"),
        voteShare: row.voteShare ? `${Number(row.voteShare).toFixed(1)}%` : "—",
        status: row.isWon ? "🏆 Won" : row.isLeading ? "🔊 Leading" : "Behind"
      };
    }
  },
  // ── Market Searches ───────────────────────────────────────────────────────────
  {
    key: "marketSearches",
    label: "Market Searches",
    icon: TrendingUp,
    idField: "id",
    canDelete: true,
    columns: [
      "id",
      "scriptName",
      "country",
      "resultsCount",
      "recommendations",
      "searchedAt",
      "createdAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getMarketSearchHistory;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      const results = Array.isArray(row.results) ? row.results : [];
      const recs = Array.isArray(row.recommendations) ? row.recommendations : [];
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        scriptName: row.scriptName ?? "—",
        country: row.country ?? "—",
        resultsCount: String(results.length),
        recommendations: String(recs.length),
        searchedAt: row.searchedAt ? new Date(Number(row.searchedAt)).toLocaleDateString("en-IN") : "—",
        createdAt: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ── Lending Items ───────────────────────────────────────────────────
  {
    key: "lendingItems",
    label: "Lending Items",
    icon: Package,
    idField: "id",
    canAdd: false,
    canDelete: false,
    columns: [
      "id",
      "itemName",
      "category",
      "lender",
      "borrower",
      "returnDate",
      "charge",
      "reminder",
      "status"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllLendingItems;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        itemName: row.itemName ?? "—",
        category: String(row.itemCategory ?? "—"),
        lender: row.lenderPhone ?? "—",
        borrower: row.borrowerPhone ?? "—",
        returnDate: row.returnDate ? new Date(Number(row.returnDate) / 1e6).toLocaleDateString(
          "en-IN"
        ) : "—",
        charge: row.charge ? `₹${row.charge}` : "—",
        reminder: String(row.reminderFrequency ?? "—"),
        status: String(row.status ?? "—")
      };
    }
  },
  // -- Community Parking Bookings --
  {
    key: "communityParkingBookings",
    label: "Community Parking",
    icon: Car,
    idField: "id",
    canAdd: true,
    canStatusUpdate: true,
    statusOptions: ["pending", "confirmed", "completed", "cancelled"],
    columns: [
      "id",
      "memberPhone",
      "communityId",
      "parkingType",
      "startDate",
      "endDate",
      "cost",
      "status"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllCommunityParkingBookings;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "pending"),
        _raw: row,
        id: sid(row.id),
        memberPhone: row.memberPhone ?? "—",
        communityId: row.communityId ?? "—",
        parkingType: String(row.parkingType ?? "—"),
        startDate: row.startDate ?? "—",
        endDate: row.endDate ?? "—",
        cost: row.cost !== void 0 ? `₹${safeCount(row.cost)}` : "—",
        status: String(row.status ?? "—")
      };
    }
  },
  // -- Community Room Bookings --
  {
    key: "communityRoomBookings",
    label: "Community Rooms",
    icon: DoorOpen,
    idField: "id",
    canAdd: true,
    canStatusUpdate: true,
    statusOptions: ["pending", "confirmed", "completed", "cancelled"],
    columns: [
      "id",
      "memberPhone",
      "communityId",
      "facilityType",
      "bookingDate",
      "timeSlot",
      "status"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllCommunityRoomBookings;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "pending"),
        _raw: row,
        id: sid(row.id),
        memberPhone: row.memberPhone ?? "—",
        communityId: row.communityId ?? "—",
        facilityType: row.facilityType ?? "—",
        bookingDate: row.bookingDate ?? "—",
        timeSlot: row.timeSlot ?? "—",
        status: String(row.status ?? "—")
      };
    }
  },
  // -- Community Food Orders --
  {
    key: "communityFoodOrders",
    label: "Community Food",
    icon: UtensilsCrossed,
    idField: "id",
    canAdd: true,
    canStatusUpdate: true,
    statusOptions: [
      "pending",
      "confirmed",
      "preparing",
      "delivered",
      "cancelled"
    ],
    columns: [
      "id",
      "buyerPhone",
      "sellerPhone",
      "communityId",
      "mealDescription",
      "cuisineType",
      "dietary",
      "quantity",
      "cost",
      "status"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllCommunityFoodOrders;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "pending"),
        _raw: row,
        id: sid(row.id),
        buyerPhone: row.buyerPhone ?? "—",
        sellerPhone: row.sellerPhone ?? "—",
        communityId: row.communityId ?? "—",
        mealDescription: String(row.mealDescription ?? "—").slice(0, 30),
        cuisineType: row.cuisineType ?? "—",
        dietary: row.dietary ?? "—",
        quantity: String(row.quantity ?? "1"),
        cost: row.cost !== void 0 ? `₹${safeCount(row.cost)}` : "—",
        status: String(row.status ?? "—")
      };
    }
  },
  // -- Community Work Orders --
  {
    key: "communityWorkOrders",
    label: "Community Services",
    icon: Wrench,
    idField: "id",
    canAdd: true,
    canStatusUpdate: true,
    statusOptions: [
      "pending",
      "assigned",
      "in-progress",
      "completed",
      "cancelled"
    ],
    columns: [
      "id",
      "memberPhone",
      "communityId",
      "serviceType",
      "description",
      "scheduledDate",
      "status"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllCommunityWorkOrders;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _status: String(row.status ?? "pending"),
        _raw: row,
        id: sid(row.id),
        memberPhone: row.memberPhone ?? "—",
        communityId: row.communityId ?? "—",
        serviceType: row.serviceType ?? "—",
        description: String(row.description ?? "—").slice(0, 40),
        scheduledDate: row.scheduledDate ?? "—",
        status: String(row.status ?? "—")
      };
    }
  },
  // -- Community Members --
  {
    key: "communityMembers",
    label: "Community Members",
    icon: Users,
    idField: "phone",
    canAdd: false,
    canDelete: true,
    columns: [
      "phone",
      "name",
      "apartment",
      "city",
      "location",
      "roles",
      "registeredAt"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllCommunityMembers;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      const roles = Array.isArray(row.roles) ? row.roles.join(", ") : String(row.roles ?? "");
      return {
        _id: row.phone ?? "",
        _raw: row,
        phone: row.phone ?? "—",
        name: row.name ?? "—",
        apartment: row.apartmentName ?? "—",
        city: row.city ?? "—",
        location: row.location ?? "—",
        roles: roles || "—",
        registeredAt: row.registeredAt ? new Date(Number(row.registeredAt) / 1e6).toLocaleDateString(
          "en-IN"
        ) : "—"
      };
    }
  },
  // ─── Bus Bookings ─────────────────────────────────────────────────────────
  {
    key: "busBookings",
    label: "Bus Bookings",
    icon: Route,
    columns: [
      "id",
      "customer",
      "route",
      "journeyDate",
      "operator",
      "fare",
      "seats",
      "status",
      "payStatus",
      "pnr",
      "env"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllBusBookings;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      const seats = Array.isArray(row.seatNumbers) ? row.seatNumbers.join(", ") : String(row.seatNumbers ?? "—");
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        customer: String(row.customerId ?? row.passengerPhone ?? "—").slice(
          0,
          14
        ),
        route: `${String(row.source ?? "—")} → ${String(row.destination ?? "—")}`,
        journeyDate: String(row.journeyDate ?? "—"),
        operator: String(row.operatorName ?? "—"),
        fare: row.fare !== void 0 ? `₹${Number(row.fare).toLocaleString("en-IN")}` : "—",
        seats,
        status: String(row.status ?? "—"),
        payStatus: String(row.paymentStatus ?? "—"),
        pnr: String(row.pnr ?? "—"),
        env: String(row.environment ?? "—")
      };
    }
  },
  // ─── Train Bookings ──────────────────────────────────────────────────────────
  {
    key: "trainBookings",
    label: "Train Bookings",
    icon: Route,
    columns: [
      "id",
      "customer",
      "route",
      "date",
      "train",
      "class",
      "fare",
      "pnr",
      "status",
      "payStatus",
      "env"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllTrainBookings;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        customer: String(row.customerId ?? "—").slice(0, 14),
        route: `${String(row.source ?? "—")} → ${String(row.destination ?? "—")}`,
        date: String(row.journeyDate ?? "—"),
        train: `${String(row.trainNumber ?? "—")} ${String(row.trainName ?? "")}`.trim(),
        class: String(row.classType ?? "—"),
        fare: row.fare !== void 0 ? `₹${Number(row.fare).toLocaleString("en-IN")}` : "—",
        pnr: String(row.pnr ?? "—"),
        status: String(row.status ?? "—"),
        payStatus: String(row.paymentStatus ?? "—"),
        env: String(row.environment ?? "—")
      };
    }
  },
  // ─── Flight Bookings ─────────────────────────────────────────────────────────
  {
    key: "flightBookings",
    label: "Flight Bookings",
    icon: Route,
    columns: [
      "id",
      "customer",
      "route",
      "date",
      "airline",
      "flight",
      "class",
      "fare",
      "bookingRef",
      "pnr",
      "status",
      "env"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllFlightBookings;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        customer: String(row.customerId ?? "—").slice(0, 14),
        route: `${String(row.source ?? "—")} → ${String(row.destination ?? "—")}`,
        date: String(row.journeyDate ?? "—"),
        airline: String(row.airline ?? "—"),
        flight: String(row.flightNumber ?? "—"),
        class: String(row.cabinClass ?? "—"),
        fare: row.fare !== void 0 ? `₹${Number(row.fare).toLocaleString("en-IN")}` : "—",
        bookingRef: String(row.bookingRef ?? "—"),
        pnr: String(row.pnr ?? "—"),
        status: String(row.status ?? "—"),
        env: String(row.environment ?? "—")
      };
    }
  },
  // ─── Mobile Recharges ─────────────────────────────────────────────────────────
  {
    key: "mobileRecharges",
    label: "Mobile Recharges",
    icon: Zap,
    columns: [
      "id",
      "customer",
      "operator",
      "number",
      "amount",
      "status",
      "txnId",
      "date"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllUtilityTransactions;
        if (typeof fn === "function") {
          const all = await fn();
          return all.filter(
            (r) => String(r.serviceType ?? "").toLowerCase().includes("recharge")
          );
        }
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        customer: String(row.customerId ?? "—").slice(0, 14),
        operator: String(row.operatorName ?? row.operatorCode ?? "—"),
        number: String(row.consumerNumber ?? "—"),
        amount: row.amount !== void 0 ? `₹${Number(row.amount).toLocaleString("en-IN")}` : "—",
        status: String(row.status ?? "—"),
        txnId: sid(row.transactionId ?? row.referenceId, 14),
        date: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ─── Bill Payments ────────────────────────────────────────────────────────────
  {
    key: "billPayments",
    label: "Bill Payments",
    icon: Package,
    columns: [
      "id",
      "customer",
      "category",
      "operator",
      "consumerNo",
      "billAmt",
      "paidAmt",
      "status",
      "txnId",
      "date"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllUtilityTransactions;
        if (typeof fn === "function") {
          const all = await fn();
          return all.filter(
            (r) => String(r.serviceType ?? "").toLowerCase().includes("bill")
          );
        }
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        customer: String(row.customerId ?? "—").slice(0, 14),
        category: String(row.serviceType ?? "—"),
        operator: String(row.operatorName ?? row.operatorCode ?? "—"),
        consumerNo: String(row.consumerNumber ?? "—"),
        billAmt: row.billAmount !== void 0 ? `₹${Number(row.billAmount).toLocaleString("en-IN")}` : "—",
        paidAmt: row.amount !== void 0 ? `₹${Number(row.amount).toLocaleString("en-IN")}` : "—",
        status: String(row.status ?? "—"),
        txnId: sid(row.transactionId ?? row.referenceId, 14),
        date: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ─── FastTag Transactions ─────────────────────────────────────────────────────
  {
    key: "fastagTransactions",
    label: "FastTag",
    icon: Car,
    columns: ["id", "customer", "vehicle", "amount", "status", "txnId", "date"],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllUtilityTransactions;
        if (typeof fn === "function") {
          const all = await fn();
          return all.filter(
            (r) => String(r.serviceType ?? "").toLowerCase().includes("fastag") || String(r.serviceType ?? "").toLowerCase().includes("fasttag")
          );
        }
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        customer: String(row.customerId ?? "—").slice(0, 14),
        vehicle: String(row.consumerNumber ?? "—"),
        amount: row.amount !== void 0 ? `₹${Number(row.amount).toLocaleString("en-IN")}` : "—",
        status: String(row.status ?? "—"),
        txnId: sid(row.transactionId ?? row.referenceId, 14),
        date: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ─── LPG Bookings ─────────────────────────────────────────────────────────────
  {
    key: "lpgBookings",
    label: "LPG Bookings",
    icon: Package,
    columns: [
      "id",
      "customer",
      "consumerNo",
      "amount",
      "status",
      "txnId",
      "date"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllUtilityTransactions;
        if (typeof fn === "function") {
          const all = await fn();
          return all.filter(
            (r) => String(r.serviceType ?? "").toLowerCase().includes("lpg") || String(r.serviceType ?? "").toLowerCase().includes("gas")
          );
        }
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        customer: String(row.customerId ?? "—").slice(0, 14),
        consumerNo: String(row.consumerNumber ?? "—"),
        amount: row.amount !== void 0 ? `₹${Number(row.amount).toLocaleString("en-IN")}` : "—",
        status: String(row.status ?? "—"),
        txnId: sid(row.transactionId ?? row.referenceId, 14),
        date: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ─── Municipality Payments ────────────────────────────────────────────────────
  {
    key: "municipalityPayments",
    label: "Municipality",
    icon: Building2,
    columns: [
      "id",
      "customer",
      "operator",
      "propertyNo",
      "amount",
      "status",
      "txnId",
      "date"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllUtilityTransactions;
        if (typeof fn === "function") {
          const all = await fn();
          return all.filter(
            (r) => String(r.serviceType ?? "").toLowerCase().includes("municipal") || String(r.serviceType ?? "").toLowerCase().includes("municipality")
          );
        }
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        customer: String(row.customerId ?? "—").slice(0, 14),
        operator: String(row.operatorName ?? row.operatorCode ?? "—"),
        propertyNo: String(row.consumerNumber ?? "—"),
        amount: row.amount !== void 0 ? `₹${Number(row.amount).toLocaleString("en-IN")}` : "—",
        status: String(row.status ?? "—"),
        txnId: sid(row.transactionId ?? row.referenceId, 14),
        date: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ─── Insurance Premiums ───────────────────────────────────────────────────────
  {
    key: "insurancePremiums",
    label: "Insurance",
    icon: Heart,
    columns: [
      "id",
      "customer",
      "policyType",
      "policyNo",
      "premium",
      "status",
      "receipt",
      "date"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllUtilityTransactions;
        if (typeof fn === "function") {
          const all = await fn();
          return all.filter(
            (r) => String(r.serviceType ?? "").toLowerCase().includes("insurance")
          );
        }
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: sid(row.id),
        customer: String(row.customerId ?? "—").slice(0, 14),
        policyType: String(row.operatorName ?? "—"),
        policyNo: String(row.consumerNumber ?? "—"),
        premium: row.amount !== void 0 ? `₹${Number(row.amount).toLocaleString("en-IN")}` : "—",
        status: String(row.status ?? "—"),
        receipt: String(row.receiptNumber ?? "—"),
        date: row.createdAt ? new Date(Number(row.createdAt)).toLocaleDateString("en-IN") : "—"
      };
    }
  },
  // ─── PaySprint API Logs ────────────────────────────────────────────────────────
  {
    key: "paysprintAPILogs",
    label: "PaySprint API Logs",
    icon: TrendingUp,
    columns: [
      "service",
      "env",
      "endpoint",
      "httpStatus",
      "latency",
      "error",
      "timestamp"
    ],
    fetchFn: async (a) => {
      try {
        const fn = a.getAllPaySprintAPILogs;
        if (typeof fn === "function") return fn();
      } catch {
      }
      return [];
    },
    mapRow: (r) => {
      const row = r;
      const isErr = Boolean(row.isError);
      return {
        _id: row.id ?? "",
        _raw: row,
        service: String(row.serviceType ?? "—"),
        env: String(row.environment ?? "—"),
        endpoint: String(row.endpoint ?? "—").slice(0, 40),
        httpStatus: String(Number(row.httpStatus ?? 0)),
        latency: row.latencyMs !== void 0 ? `${Number(row.latencyMs)}ms` : "—",
        error: isErr ? String(row.errorMessage ?? "Error").slice(0, 40) : "—",
        timestamp: row.createdAt ? new Date(Number(row.createdAt)).toLocaleString("en-IN") : "—"
      };
    }
  }
];
const _ll = {
  getLanguageCoursesForDataExplorer: async () => [],
  getLessonsForDataExplorer: async () => [],
  getEnrollmentsForDataExplorer: async () => [],
  getCourseApprovalsForDataExplorer: async () => [],
  getWordDefinitionsForDataExplorer: async () => [],
  getDailyLessonsForDataExplorer: async () => []
};
const LANGUAGE_LEARNING_ENTITIES = [
  {
    key: "language_courses",
    label: "Language Courses",
    icon: BookOpen,
    columns: [
      "id",
      "title",
      "creatorPhone",
      "languagePair",
      "price",
      "status",
      "enrollmentCount",
      "createdDate"
    ],
    fetchFn: async (_a) => _ll.getLanguageCoursesForDataExplorer(),
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: String(row.id ?? "").slice(0, 8),
        title: String(row.title ?? "—"),
        creatorPhone: String(row.creatorPhone ?? "—"),
        languagePair: String(row.languagePair ?? "—"),
        price: row.price !== void 0 ? `₹${Number(row.price)}` : "Free",
        status: String(row.status ?? "—"),
        enrollmentCount: Number(row.enrollmentCount ?? 0),
        createdDate: row.createdDate ? new Date(Number(row.createdDate)).toLocaleDateString("en-IN") : "—"
      };
    },
    canAdd: true,
    canEdit: true,
    canDelete: true,
    canStatusUpdate: true,
    statusOptions: ["pending", "approved", "rejected", "draft"]
  },
  {
    key: "language_lessons",
    label: "Lessons",
    icon: Library,
    columns: ["id", "courseId", "title", "order", "wordCount"],
    fetchFn: async (_a) => _ll.getLessonsForDataExplorer(),
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: String(row.id ?? "").slice(0, 8),
        courseId: String(row.courseId ?? "—").slice(0, 8),
        title: String(row.title ?? "—"),
        order: Number(row.order ?? 0),
        wordCount: Number(row.wordCount ?? 0)
      };
    },
    canAdd: true,
    canEdit: true,
    canDelete: true
  },
  {
    key: "language_enrollments",
    label: "Enrollments",
    icon: Users,
    columns: [
      "id",
      "userPhone",
      "courseId",
      "enrollmentDate",
      "progressPercent",
      "paymentStatus"
    ],
    fetchFn: async (_a) => _ll.getEnrollmentsForDataExplorer(),
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: String(row.id ?? "").slice(0, 8),
        userPhone: String(row.userPhone ?? "—"),
        courseId: String(row.courseId ?? "—").slice(0, 8),
        enrollmentDate: row.enrollmentDate ? new Date(Number(row.enrollmentDate)).toLocaleDateString("en-IN") : "—",
        progressPercent: `${Number(row.progressPercent ?? 0)}%`,
        paymentStatus: String(row.paymentStatus ?? "—")
      };
    },
    canAdd: false,
    canEdit: true,
    canDelete: true
  },
  {
    key: "language_approvals",
    label: "Course Approvals",
    icon: CircleCheckBig,
    columns: [
      "id",
      "courseId",
      "adminNotes",
      "approverId",
      "approvalDate",
      "status"
    ],
    fetchFn: async (_a) => _ll.getCourseApprovalsForDataExplorer(),
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: String(row.id ?? "").slice(0, 8),
        courseId: String(row.courseId ?? "—").slice(0, 8),
        adminNotes: String(row.adminNotes ?? "—"),
        approverId: String(row.approverId ?? "—").slice(0, 8),
        approvalDate: row.approvalDate ? new Date(Number(row.approvalDate)).toLocaleDateString("en-IN") : "—",
        status: String(row.status ?? "—")
      };
    },
    canAdd: false,
    canEdit: false,
    canDelete: false
  },
  {
    key: "word_definitions",
    label: "Word Definitions",
    icon: Globe,
    columns: ["id", "word", "language", "ancientTranslation", "ipa"],
    fetchFn: async (_a) => _ll.getWordDefinitionsForDataExplorer(),
    mapRow: (r) => {
      var _a;
      const row = r;
      const translations = row.translations;
      const ancientTranslation = String(
        row.ancientTranslation ?? ((_a = translations == null ? void 0 : translations.find(
          ([lang]) => ["Sanskrit", "Latin", "Old Arabic"].includes(lang)
        )) == null ? void 0 : _a[1]) ?? "—"
      );
      return {
        _id: row.id ?? "",
        _raw: row,
        id: String(row.id ?? "").slice(0, 8),
        word: String(row.word ?? "—"),
        language: String(row.language ?? "—"),
        ancientTranslation,
        ipa: String(row.ipa ?? row.pronunciation ?? "—")
      };
    },
    canAdd: true,
    canEdit: true,
    canDelete: true
  },
  {
    key: "daily_lessons",
    label: "Daily Lessons",
    icon: Flame,
    columns: [
      "id",
      "userPhone",
      "languagePair",
      "lessonDate",
      "isCompleted",
      "streakDate"
    ],
    fetchFn: async (_a) => _ll.getDailyLessonsForDataExplorer(),
    mapRow: (r) => {
      const row = r;
      return {
        _id: row.id ?? "",
        _raw: row,
        id: String(row.id ?? "").slice(0, 8),
        userPhone: String(row.userPhone ?? "—"),
        languagePair: String(row.languagePair ?? "—"),
        lessonDate: row.lessonDate ? new Date(Number(row.lessonDate)).toLocaleDateString("en-IN") : "—",
        isCompleted: row.isCompleted ? "✅ Yes" : "⏳ No",
        streakDate: row.streakDate ? new Date(Number(row.streakDate)).toLocaleDateString("en-IN") : "—"
      };
    },
    canAdd: false,
    canEdit: true,
    canDelete: true
  }
];
function Modal({
  title,
  onClose,
  children
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-2xl shadow-2xl w-full max-w-md mx-4", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-semibold text-sm text-foreground", children: title }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "text-muted-foreground hover:text-foreground transition-colors",
          "aria-label": "Close",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-4 h-4" })
        }
      )
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "px-5 py-4", children })
  ] }) });
}
function InlineStatusSelect({
  currentStatus,
  options,
  onSave,
  saving
}) {
  const [val, setVal] = reactExports.useState(currentStatus);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "select",
      {
        value: val,
        onChange: (e) => setVal(e.target.value),
        className: "text-xs h-7 px-2 rounded border border-border bg-muted/30 text-foreground",
        children: options.map((o) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: o, children: o }, o))
      }
    ),
    val !== currentStatus && /* @__PURE__ */ jsxRuntimeExports.jsx(
      Button,
      {
        size: "sm",
        className: "h-6 text-[10px] px-2",
        onClick: () => onSave(val),
        disabled: saving,
        children: saving ? "…" : "Save"
      }
    )
  ] });
}
function CityForm({
  initial,
  onSave,
  saving
}) {
  const [name, setName] = reactExports.useState(
    String((initial == null ? void 0 : initial.cityName) ?? (initial == null ? void 0 : initial.name) ?? "")
  );
  const [pincodes, setPincodes] = reactExports.useState(String((initial == null ? void 0 : initial.pincode) ?? ""));
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({ name, pincodes });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "City Name *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: name,
              onChange: (e) => setName(e.target.value),
              placeholder: "e.g. Mumbai",
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Pincode(s) (comma-separated)" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: pincodes,
              onChange: (e) => setPincodes(e.target.value),
              placeholder: "e.g. 400001, 400002",
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : "Save City" })
      ]
    }
  );
}
function CityControlForm({
  onSave,
  saving
}) {
  const [cityId, setCityId] = reactExports.useState("");
  const [moduleName, setModuleName] = reactExports.useState("");
  const [enabled, setEnabled] = reactExports.useState(true);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({ cityId, moduleName, enabled });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "City ID *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: cityId,
              onChange: (e) => setCityId(e.target.value),
              placeholder: "City ID",
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Module Name *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: moduleName,
              onChange: (e) => setModuleName(e.target.value),
              placeholder: "e.g. marketplace",
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Switch,
            {
              id: "ctrl-enabled",
              checked: enabled,
              onCheckedChange: setEnabled
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { htmlFor: "ctrl-enabled", className: "text-xs", children: "Enabled" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : "Save Control" })
      ]
    }
  );
}
function MarketplaceItemForm({
  initial,
  onSave,
  saving
}) {
  const isAdd = !(initial == null ? void 0 : initial._id);
  const [title, setTitle] = reactExports.useState(String((initial == null ? void 0 : initial.title) ?? ""));
  const [price, setPrice] = reactExports.useState(String(safeCount(initial == null ? void 0 : initial.price)));
  const [category, setCategory] = reactExports.useState(
    String((initial == null ? void 0 : initial.category) ?? "General")
  );
  const [listingType, setListingType] = reactExports.useState(
    String((initial == null ? void 0 : initial.rentOrSale) ?? (initial == null ? void 0 : initial.listingType) ?? "sell")
  );
  const [condition, setCondition] = reactExports.useState("used");
  const [status, setStatus] = reactExports.useState(
    (initial == null ? void 0 : initial.active) === "Active" ? "active" : "inactive"
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        if (isAdd) {
          onSave({
            title,
            price: Number(price),
            category,
            listingType,
            condition,
            yearOfManufacture: BigInt((/* @__PURE__ */ new Date()).getFullYear()),
            instagramPhotoLink: "",
            invoiceAvailable: false,
            createdBy: "admin"
          });
        } else {
          onSave({
            id: String((initial == null ? void 0 : initial._id) ?? ""),
            title,
            price: Number(price),
            status
          });
        }
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Title *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: title,
              onChange: (e) => setTitle(e.target.value),
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Price (₹) *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "number",
              value: price,
              onChange: (e) => setPrice(e.target.value),
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        isAdd && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Category" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Input,
              {
                value: category,
                onChange: (e) => setCategory(e.target.value),
                placeholder: "e.g. Electronics",
                className: "h-8 text-sm"
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Sell or Rent" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "select",
              {
                value: listingType,
                onChange: (e) => setListingType(e.target.value),
                className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "sell", children: "Sell" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "rent", children: "Rent" })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Condition" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "select",
              {
                value: condition,
                onChange: (e) => setCondition(e.target.value),
                className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "new", children: "New" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "used", children: "Used" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "refurbished", children: "Refurbished" })
                ]
              }
            )
          ] })
        ] }),
        !isAdd && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Status" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "select",
            {
              value: status,
              onChange: (e) => setStatus(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "active", children: "Active" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "inactive", children: "Inactive" })
              ]
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : isAdd ? "Add Item" : "Save Item" })
      ]
    }
  );
}
function RestockOrderForm({
  onSave,
  saving
}) {
  const [merchantId, setMerchantId] = reactExports.useState("");
  const [supplierName, setSupplierName] = reactExports.useState("");
  const [itemName, setItemName] = reactExports.useState("");
  const [quantity, setQuantity] = reactExports.useState("1");
  const [notes, setNotes] = reactExports.useState("");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          merchantId,
          supplierName,
          itemName,
          quantity: Number(quantity),
          notes
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Merchant ID *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: merchantId,
              onChange: (e) => setMerchantId(e.target.value),
              placeholder: "e.g. merchant-001",
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Supplier Name *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: supplierName,
              onChange: (e) => setSupplierName(e.target.value),
              placeholder: "e.g. ABC Distributors",
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Item Name *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: itemName,
              onChange: (e) => setItemName(e.target.value),
              placeholder: "e.g. Rice 25kg",
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Quantity *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "number",
              value: quantity,
              onChange: (e) => setQuantity(e.target.value),
              min: "1",
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Notes" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: notes,
              onChange: (e) => setNotes(e.target.value),
              placeholder: "Optional notes",
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : "Create Restock Order" })
      ]
    }
  );
}
function ModerationFlagForm({
  onSave,
  saving
}) {
  const [contentId, setContentId] = reactExports.useState("");
  const [contentType, setContentType] = reactExports.useState("product");
  const [reportedBy, setReportedBy] = reactExports.useState("");
  const [reason, setReason] = reactExports.useState("");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({ contentId, contentType, reportedBy, reason });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Content ID *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: contentId,
              onChange: (e) => setContentId(e.target.value),
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Content Type" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              value: contentType,
              onChange: (e) => setContentType(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: [
                "product",
                "order",
                "user",
                "merchant",
                "job",
                "property",
                "event"
              ].map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: t, children: t }, t))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Reported By (phone)" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: reportedBy,
              onChange: (e) => setReportedBy(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Reason *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: reason,
              onChange: (e) => setReason(e.target.value),
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Flagging…" : "Flag Content" })
      ]
    }
  );
}
function RecipeForm({
  onSave,
  saving
}) {
  const [title, setTitle] = reactExports.useState("");
  const [category, setCategory] = reactExports.useState("General");
  const [ingredients, setIngredients] = reactExports.useState("");
  const [instructions, setInstructions] = reactExports.useState("");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({ title, category, ingredients, instructions });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Recipe Title *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: title,
              onChange: (e) => setTitle(e.target.value),
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Category" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: category,
              onChange: (e) => setCategory(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Ingredients (one per line)" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "textarea",
            {
              value: ingredients,
              onChange: (e) => setIngredients(e.target.value),
              className: "w-full min-h-[80px] px-3 py-2 rounded-md border border-border bg-muted/30 text-sm text-foreground resize-none",
              placeholder: "e.g. 2 cups flour\n1 tsp salt"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Instructions" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "textarea",
            {
              value: instructions,
              onChange: (e) => setInstructions(e.target.value),
              className: "w-full min-h-[80px] px-3 py-2 rounded-md border border-border bg-muted/30 text-sm text-foreground resize-none",
              placeholder: "Step by step instructions…"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : "Save Recipe" })
      ]
    }
  );
}
function HealthcareProviderForm({
  initial,
  onSave,
  saving
}) {
  const [name, setName] = reactExports.useState(
    String((initial == null ? void 0 : initial.Name) ?? (initial == null ? void 0 : initial.name) ?? "")
  );
  const [specialization, setSpecialization] = reactExports.useState(
    String((initial == null ? void 0 : initial.Specialization) ?? (initial == null ? void 0 : initial.specialization) ?? "")
  );
  const [location, setLocation] = reactExports.useState(
    String((initial == null ? void 0 : initial.Location) ?? (initial == null ? void 0 : initial.location) ?? "")
  );
  const [phone, setPhone] = reactExports.useState(
    String((initial == null ? void 0 : initial.Phone) ?? (initial == null ? void 0 : initial.phone) ?? "")
  );
  const [availableDays, setAvailableDays] = reactExports.useState(
    String((initial == null ? void 0 : initial["Available Days"]) ?? (initial == null ? void 0 : initial.availableDays) ?? "")
  );
  const [fee, setFee] = reactExports.useState(String(safeCount(initial == null ? void 0 : initial.fee)));
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          name,
          specialization,
          location,
          phone,
          availableDays,
          fee: Number(fee)
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Name *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: name,
              onChange: (e) => setName(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "Dr. Ravi Kumar"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Specialization *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: specialization,
              onChange: (e) => setSpecialization(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "General Physician"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Location" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: location,
              onChange: (e) => setLocation(e.target.value),
              className: "h-8 text-sm",
              placeholder: "Mumbai"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Phone" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: phone,
              onChange: (e) => setPhone(e.target.value),
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Available Days" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: availableDays,
              onChange: (e) => setAvailableDays(e.target.value),
              className: "h-8 text-sm",
              placeholder: "Mon-Sat"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Consultation Fee (₹)" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "number",
              value: fee,
              onChange: (e) => setFee(e.target.value),
              className: "h-8 text-sm",
              min: "0"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : (initial == null ? void 0 : initial._id) ? "Save Changes" : "Add Provider" })
      ]
    }
  );
}
const TOUR_TYPES = [
  "Day Trip",
  "Multi-Day",
  "Adventure",
  "Cultural",
  "Religious",
  "Wildlife",
  "Beach",
  "Hill Station",
  "Other"
];
function TourOperatorForm({
  initial,
  onSave,
  saving
}) {
  const [name, setName] = reactExports.useState(
    String((initial == null ? void 0 : initial.Name) ?? (initial == null ? void 0 : initial.name) ?? "")
  );
  const [destination, setDestination] = reactExports.useState(
    String((initial == null ? void 0 : initial.Destination) ?? (initial == null ? void 0 : initial.destination) ?? "")
  );
  const [tourType, setTourType] = reactExports.useState(
    String((initial == null ? void 0 : initial.Type) ?? (initial == null ? void 0 : initial.tourType) ?? TOUR_TYPES[0])
  );
  const [duration, setDuration] = reactExports.useState(
    String((initial == null ? void 0 : initial.Duration) ?? (initial == null ? void 0 : initial.duration) ?? "")
  );
  const [price, setPrice] = reactExports.useState(String(safeCount(initial == null ? void 0 : initial.price)));
  const [phone, setPhone] = reactExports.useState(
    String((initial == null ? void 0 : initial.Phone) ?? (initial == null ? void 0 : initial.phone) ?? "")
  );
  const [description, setDescription] = reactExports.useState(
    String((initial == null ? void 0 : initial.description) ?? "")
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          name,
          destination,
          tourType,
          duration,
          price: Number(price),
          phone,
          description
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Operator Name *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: name,
              onChange: (e) => setName(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "Explore India Tours"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Destination *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: destination,
              onChange: (e) => setDestination(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "Goa, Kerala, Himachal…"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Tour Type" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              value: tourType,
              onChange: (e) => setTourType(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: TOUR_TYPES.map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: t, children: t }, t))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Duration" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: duration,
              onChange: (e) => setDuration(e.target.value),
              className: "h-8 text-sm",
              placeholder: "3 Days / 2 Nights"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Price (₹ per person)" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "number",
              value: price,
              onChange: (e) => setPrice(e.target.value),
              className: "h-8 text-sm",
              min: "0"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Phone" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: phone,
              onChange: (e) => setPhone(e.target.value),
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Description" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: description,
              onChange: (e) => setDescription(e.target.value),
              className: "h-8 text-sm",
              placeholder: "Brief tour details…"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : (initial == null ? void 0 : initial._id) ? "Save Changes" : "Add Tour" })
      ]
    }
  );
}
const PROF_SERVICE_TYPES = [
  "Doctor",
  "Nurse",
  "Physiotherapist",
  "Plumber",
  "Electrician",
  "Carpenter",
  "Painter",
  "Cleaner",
  "Driver",
  "Cook",
  "Teacher",
  "Personal Trainer",
  "Yoga Instructor",
  "Masseur",
  "Other"
];
function ProfessionalServiceForm({
  initial,
  onSave,
  saving
}) {
  const [name, setName] = reactExports.useState(
    String((initial == null ? void 0 : initial.Name) ?? (initial == null ? void 0 : initial.name) ?? "")
  );
  const [serviceType, setServiceType] = reactExports.useState(
    String(
      (initial == null ? void 0 : initial["Service Type"]) ?? (initial == null ? void 0 : initial.serviceType) ?? PROF_SERVICE_TYPES[0]
    )
  );
  const [location, setLocation] = reactExports.useState(
    String((initial == null ? void 0 : initial.Location) ?? (initial == null ? void 0 : initial.location) ?? "")
  );
  const [phone, setPhone] = reactExports.useState(
    String((initial == null ? void 0 : initial.Phone) ?? (initial == null ? void 0 : initial.phone) ?? "")
  );
  const [experience, setExperience] = reactExports.useState(
    String((initial == null ? void 0 : initial.Experience) ?? (initial == null ? void 0 : initial.experience) ?? "")
  );
  const [hourlyRate, setHourlyRate] = reactExports.useState(
    String(safeCount(initial == null ? void 0 : initial.hourlyRate))
  );
  const [description, setDescription] = reactExports.useState(
    String((initial == null ? void 0 : initial.description) ?? "")
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          name,
          serviceType,
          location,
          phone,
          experience,
          hourlyRate: Number(hourlyRate),
          description
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Full Name *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: name,
              onChange: (e) => setName(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "Ramesh Gupta"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Service Type" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              value: serviceType,
              onChange: (e) => setServiceType(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: PROF_SERVICE_TYPES.map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: t, children: t }, t))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Location" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: location,
              onChange: (e) => setLocation(e.target.value),
              className: "h-8 text-sm",
              placeholder: "Mumbai"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Phone" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: phone,
              onChange: (e) => setPhone(e.target.value),
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Experience" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: experience,
              onChange: (e) => setExperience(e.target.value),
              className: "h-8 text-sm",
              placeholder: "5 years"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Hourly Rate (₹)" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "number",
              value: hourlyRate,
              onChange: (e) => setHourlyRate(e.target.value),
              className: "h-8 text-sm",
              min: "0"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Description" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: description,
              onChange: (e) => setDescription(e.target.value),
              className: "h-8 text-sm",
              placeholder: "Brief description of services…"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : (initial == null ? void 0 : initial._id) ? "Save Changes" : "Add Service" })
      ]
    }
  );
}
function AdhocJobForm({
  onSave,
  saving
}) {
  const [title, setTitle] = reactExports.useState("");
  const [description, setDescription] = reactExports.useState("");
  const [location, setLocation] = reactExports.useState("");
  const [budget, setBudget] = reactExports.useState("0");
  const [category, setCategory] = reactExports.useState("General");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          title,
          description,
          location,
          budget: Number(budget),
          category
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Job Title *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: title,
              onChange: (e) => setTitle(e.target.value),
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Description" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: description,
              onChange: (e) => setDescription(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Location" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: location,
              onChange: (e) => setLocation(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Budget (₹)" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "number",
              value: budget,
              onChange: (e) => setBudget(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Category" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: category,
              onChange: (e) => setCategory(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Posting…" : "Post Job" })
      ]
    }
  );
}
function DonationForm({
  onSave,
  saving
}) {
  const [category, setCategory] = reactExports.useState("Food Items");
  const [description, setDescription] = reactExports.useState("");
  const [quantity, setQuantity] = reactExports.useState("1");
  const [location, setLocation] = reactExports.useState("");
  const [donorName, setDonorName] = reactExports.useState("");
  const [donorPhone, setDonorPhone] = reactExports.useState("");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          category,
          description,
          quantity,
          location,
          donorName,
          donorPhone
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Category *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              value: category,
              onChange: (e) => setCategory(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: ["Food Items", "Clothes", "Books"].map((c) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: c, children: c }, c))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Description *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: description,
              onChange: (e) => setDescription(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "e.g. 5kg rice, good condition"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Quantity" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: quantity,
              onChange: (e) => setQuantity(e.target.value),
              className: "h-8 text-sm",
              placeholder: "e.g. 10 kg / 5 books"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Location *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: location,
              onChange: (e) => setLocation(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "City / Area"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Donor Name *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: donorName,
              onChange: (e) => setDonorName(e.target.value),
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Donor Phone *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: donorPhone,
              onChange: (e) => setDonorPhone(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Adding…" : "Add Donation" })
      ]
    }
  );
}
function DonationRequestForm({
  onSave,
  saving
}) {
  const [category, setCategory] = reactExports.useState("Food Items");
  const [description, setDescription] = reactExports.useState("");
  const [quantityNeeded, setQuantityNeeded] = reactExports.useState("1");
  const [location, setLocation] = reactExports.useState("");
  const [requesterName, setRequesterName] = reactExports.useState("");
  const [requesterPhone, setRequesterPhone] = reactExports.useState("");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          category,
          description,
          quantityNeeded,
          location,
          requesterName,
          requesterPhone
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Category *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              value: category,
              onChange: (e) => setCategory(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: ["Food Items", "Clothes", "Books"].map((c) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: c, children: c }, c))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Description *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: description,
              onChange: (e) => setDescription(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "What you need"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Quantity Needed" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: quantityNeeded,
              onChange: (e) => setQuantityNeeded(e.target.value),
              className: "h-8 text-sm",
              placeholder: "e.g. 5 kg / 3 books"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Location *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: location,
              onChange: (e) => setLocation(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "City / Area"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Requester Name *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: requesterName,
              onChange: (e) => setRequesterName(e.target.value),
              required: true,
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Requester Phone *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: requesterPhone,
              onChange: (e) => setRequesterPhone(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Requesting…" : "Submit Request" })
      ]
    }
  );
}
function CommunityParkingForm({
  onSave,
  saving
}) {
  const [memberPhone, setMemberPhone] = reactExports.useState("");
  const [communityId, setCommunityId] = reactExports.useState("");
  const [parkingType, setParkingType] = reactExports.useState("open");
  const [startDate, setStartDate] = reactExports.useState("");
  const [endDate, setEndDate] = reactExports.useState("");
  const [cost, setCost] = reactExports.useState("0");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          memberPhone,
          communityId,
          parkingType,
          startDate,
          endDate,
          cost: Number(cost)
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Member Phone *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: memberPhone,
              onChange: (e) => setMemberPhone(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Community ID (city:location) *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: communityId,
              onChange: (e) => setCommunityId(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "Gandhidham:Sector7"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Parking Type" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "select",
            {
              value: parkingType,
              onChange: (e) => setParkingType(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "open", children: "Open" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "covered", children: "Covered" })
              ]
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Start Date" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "date",
              value: startDate,
              onChange: (e) => setStartDate(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "End Date" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "date",
              value: endDate,
              onChange: (e) => setEndDate(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Monthly Cost (₹)" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "number",
              value: cost,
              onChange: (e) => setCost(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : "Create Booking" })
      ]
    }
  );
}
function CommunityRoomForm({
  onSave,
  saving
}) {
  const [memberPhone, setMemberPhone] = reactExports.useState("");
  const [communityId, setCommunityId] = reactExports.useState("");
  const [facilityType, setFacilityType] = reactExports.useState("Clubhouse");
  const [bookingDate, setBookingDate] = reactExports.useState("");
  const [timeSlot, setTimeSlot] = reactExports.useState("09:00-11:00");
  const [description, setDescription] = reactExports.useState("");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          memberPhone,
          communityId,
          facilityType,
          bookingDate,
          timeSlot,
          description
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Member Phone *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: memberPhone,
              onChange: (e) => setMemberPhone(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Community ID (city:location) *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: communityId,
              onChange: (e) => setCommunityId(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "Gandhidham:Sector7"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Facility Type" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              value: facilityType,
              onChange: (e) => setFacilityType(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: [
                "Clubhouse",
                "Party Hall",
                "Meeting Room",
                "Gym",
                "Swimming Pool",
                "Terrace",
                "Garden"
              ].map((f) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: f, children: f }, f))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Booking Date" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "date",
              value: bookingDate,
              onChange: (e) => setBookingDate(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Time Slot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: timeSlot,
              onChange: (e) => setTimeSlot(e.target.value),
              className: "h-8 text-sm",
              placeholder: "e.g. 10:00-12:00"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Description / Purpose" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: description,
              onChange: (e) => setDescription(e.target.value),
              className: "h-8 text-sm",
              placeholder: "Birthday party, meeting…"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : "Create Booking" })
      ]
    }
  );
}
function CommunityFoodOrderForm({
  onSave,
  saving
}) {
  const [buyerPhone, setBuyerPhone] = reactExports.useState("");
  const [sellerPhone, setSellerPhone] = reactExports.useState("");
  const [communityId, setCommunityId] = reactExports.useState("");
  const [mealDescription, setMealDescription] = reactExports.useState("");
  const [cuisineType, setCuisineType] = reactExports.useState("Indian");
  const [dietary, setDietary] = reactExports.useState("none");
  const [deliveryTime, setDeliveryTime] = reactExports.useState("");
  const [quantity, setQuantity] = reactExports.useState("1");
  const [cost, setCost] = reactExports.useState("0");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          buyerPhone,
          sellerPhone,
          communityId,
          mealDescription,
          cuisineType,
          dietary,
          deliveryTime,
          quantity: Number(quantity),
          cost: Number(cost)
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Buyer Phone *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: buyerPhone,
              onChange: (e) => setBuyerPhone(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Seller Phone *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: sellerPhone,
              onChange: (e) => setSellerPhone(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Community ID (city:location) *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: communityId,
              onChange: (e) => setCommunityId(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "Gandhidham:Sector7"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Meal Description *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: mealDescription,
              onChange: (e) => setMealDescription(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "e.g. Dal rice, roti sabzi"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Cuisine Type" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              value: cuisineType,
              onChange: (e) => setCuisineType(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: [
                "Indian",
                "Chinese",
                "Continental",
                "South Indian",
                "North Indian",
                "Street Food"
              ].map((c) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: c, children: c }, c))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Dietary" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              value: dietary,
              onChange: (e) => setDietary(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: ["none", "vegetarian", "vegan", "jain", "non-vegetarian"].map(
                (d) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: d, children: d }, d)
              )
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Delivery Time" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: deliveryTime,
              onChange: (e) => setDeliveryTime(e.target.value),
              className: "h-8 text-sm",
              placeholder: "e.g. 1:00 PM"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Quantity" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "number",
              min: "1",
              value: quantity,
              onChange: (e) => setQuantity(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Cost (₹)" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "number",
              value: cost,
              onChange: (e) => setCost(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : "Place Food Order" })
      ]
    }
  );
}
function CommunityWorkOrderForm({
  onSave,
  saving
}) {
  const [memberPhone, setMemberPhone] = reactExports.useState("");
  const [communityId, setCommunityId] = reactExports.useState("");
  const [serviceType, setServiceType] = reactExports.useState("Plumbing");
  const [description, setDescription] = reactExports.useState("");
  const [scheduledDate, setScheduledDate] = reactExports.useState("");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        onSave({
          memberPhone,
          communityId,
          serviceType,
          description,
          scheduledDate
        });
      },
      className: "space-y-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Member Phone *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: memberPhone,
              onChange: (e) => setMemberPhone(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "+91 98765 43210"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Community ID (city:location) *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: communityId,
              onChange: (e) => setCommunityId(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "Gandhidham:Sector7"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Service Type *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              value: serviceType,
              onChange: (e) => setServiceType(e.target.value),
              className: "w-full h-8 px-2 rounded border border-border bg-muted/30 text-sm text-foreground",
              children: [
                "Plumbing",
                "Electrical",
                "Carpentry",
                "Cleaning",
                "Pest Control",
                "Painting",
                "CCTV/Security",
                "Lift Maintenance",
                "Garden Maintenance",
                "Society Manager"
              ].map((s) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: s, children: s }, s))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Description *" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              value: description,
              onChange: (e) => setDescription(e.target.value),
              required: true,
              className: "h-8 text-sm",
              placeholder: "Describe the issue or service needed"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs mb-1 block", children: "Scheduled Date" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Input,
            {
              type: "date",
              value: scheduledDate,
              onChange: (e) => setScheduledDate(e.target.value),
              className: "h-8 text-sm"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: saving, className: "w-full", children: saving ? "Saving…" : "Create Work Order" })
      ]
    }
  );
}
function MarketSearchDetailModal({
  row,
  onClose
}) {
  const raw = row._raw;
  const results = Array.isArray(raw == null ? void 0 : raw.results) ? raw.results : [];
  const recs = Array.isArray(raw == null ? void 0 : raw.recommendations) ? raw.recommendations : [];
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-2xl shadow-2xl w-full max-w-2xl mx-4 max-h-[85vh] flex flex-col", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between px-5 py-4 border-b border-border flex-shrink-0", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-semibold text-sm text-foreground", children: String((raw == null ? void 0 : raw.scriptName) ?? "Market Search") }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-muted-foreground", children: [
          String((raw == null ? void 0 : raw.country) ?? ""),
          " · ",
          String(row.searchedAt ?? "")
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "text-muted-foreground hover:text-foreground transition-colors",
          "aria-label": "Close",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-4 h-4" })
        }
      )
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ScrollArea, { className: "flex-1 min-h-0 px-5 py-4", children: [
      results.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-4", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("h4", { className: "text-xs font-semibold text-muted-foreground uppercase tracking-wider mb-2", children: [
          "Results (",
          results.length,
          ")"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-2", children: results.map((r, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "p-3 rounded-lg bg-muted/30 border border-border text-xs",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-start mb-1", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold text-foreground", children: String(r.symbol ?? r.name ?? r.ticker ?? "—") }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: String(r.exchange ?? "") })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-4 text-muted-foreground", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                  "Price:",
                  " ",
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { className: "text-foreground", children: String(r.price ?? r.currentPrice ?? "—") })
                ] }),
                Boolean(r.change != null) && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                  "Change:",
                  " ",
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "strong",
                    {
                      className: String(r.change).startsWith("-") ? "text-destructive" : "text-green-600",
                      children: String(r.change)
                    }
                  )
                ] }),
                Boolean(r.type != null) && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                  "Type: ",
                  String(r.type)
                ] })
              ] })
            ]
          },
          `res-${i}-${String(r.symbol ?? r.name ?? "")}`
        )) })
      ] }),
      recs.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("h4", { className: "text-xs font-semibold text-muted-foreground uppercase tracking-wider mb-2", children: [
          "Recommendations (",
          recs.length,
          ")"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-2", children: recs.map((rec, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "p-3 rounded-lg border text-xs",
            style: {
              borderColor: String(rec.action) === "buy" ? "oklch(0.7 0.2 145)" : String(rec.action) === "sell" ? "oklch(0.65 0.22 25)" : void 0
            },
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold text-foreground", children: String(rec.symbol ?? rec.asset ?? "—") }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  Badge,
                  {
                    variant: String(rec.action).toLowerCase() === "buy" ? "default" : "destructive",
                    className: "text-[10px] uppercase",
                    children: String(rec.action ?? "—")
                  }
                )
              ] }),
              Boolean(rec.stopLoss != null) && /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-muted-foreground mt-1", children: [
                "Stop Loss: ",
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: String(rec.stopLoss) })
              ] }),
              Boolean(rec.reason != null) && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground mt-0.5", children: String(rec.reason) })
            ]
          },
          `rec-${i}-${String(rec.symbol ?? rec.asset ?? "")}`
        )) })
      ] }),
      results.length === 0 && recs.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm text-center py-8", children: "No results stored for this search." })
    ] })
  ] }) });
}
function DataExplorerPage() {
  const { actor } = useBackendActor();
  const llHook = useLanguageLearning();
  Object.assign(_ll, {
    getLanguageCoursesForDataExplorer: llHook.getLanguageCoursesForDataExplorer,
    getLessonsForDataExplorer: llHook.getLessonsForDataExplorer,
    getEnrollmentsForDataExplorer: llHook.getEnrollmentsForDataExplorer,
    getCourseApprovalsForDataExplorer: llHook.getCourseApprovalsForDataExplorer,
    getWordDefinitionsForDataExplorer: llHook.getWordDefinitionsForDataExplorer,
    getDailyLessonsForDataExplorer: llHook.getDailyLessonsForDataExplorer
  });
  const [selectedKey, setSelectedKey] = reactExports.useState("users");
  const [search, setSearch] = reactExports.useState("");
  const [sourceFilter, setSourceFilter] = reactExports.useState("All Sources");
  const [loading, setLoading] = reactExports.useState(false);
  const [rows, setRows] = reactExports.useState([]);
  const [rawData, setRawData] = reactExports.useState([]);
  const [fetched, setFetched] = reactExports.useState(false);
  const [activeTab, setActiveTab] = reactExports.useState("table");
  const [modalMode, setModalMode] = reactExports.useState(null);
  const [modalRow, setModalRow] = reactExports.useState(
    null
  );
  const [saving, setSaving] = reactExports.useState(false);
  const [statusSavingId, setStatusSavingId] = reactExports.useState(null);
  const [marketSearchDetailRow, setMarketSearchDetailRow] = reactExports.useState(null);
  const entity = [...ENTITIES, ...LANGUAGE_LEARNING_ENTITIES].find(
    (e) => e.key === selectedKey
  );
  const addCity = useAddCity();
  const updateCity = useUpdateCity();
  const deleteCity = useDeleteCity();
  const saveCityControl = useSaveCityControl();
  const createRestockOrder = useCreateRestockOrderAdmin();
  const updateMarketplaceItem = useUpdateMarketplaceItemAdmin();
  const createMarketplaceItem = useCreateMarketplaceItem();
  const deleteMarketplaceItem = useDeleteMarketplaceItemAdmin();
  const deleteRestockOrder = useDeleteRestockOrderAdmin();
  const updateRestockStatus = useUpdateRestockStatus();
  const createModerationItem = useCreateModerationItemAdmin();
  const updateModerationStatus = useUpdateModerationItemStatusAdmin();
  const deleteModerationItem = useDeleteModerationItemAdmin();
  const updateTicketStatus = useUpdateSupportTicketStatus();
  const deleteTicket = useDeleteSupportTicketAdmin();
  const createRecipe = useCreateRecipe();
  const deleteRecipe = useDeleteRecipe();
  const createAdhocJob = useCreateAdhocJobAdmin();
  const updateAdhocJobStatus = useUpdateAdhocJobStatusAdmin();
  const deleteAdhocJob = useDeleteAdhocJobAdmin();
  async function fetchEntity(key) {
    if (!actor) {
      ue.error(
        "Backend not connected yet — please wait a moment and try again"
      );
      return;
    }
    setLoading(true);
    setFetched(false);
    setRows([]);
    setRawData([]);
    try {
      const def = [...ENTITIES, ...LANGUAGE_LEARNING_ENTITIES].find(
        (e) => e.key === key
      );
      const actorAny = actor;
      const raw = await def.fetchFn(actorAny);
      const safeRaw = Array.isArray(raw) ? raw : [];
      setRawData(safeRaw);
      setRows(safeRaw.map(def.mapRow));
      setFetched(true);
    } catch (err) {
      const msg = err instanceof Error ? err.message : "Unknown error";
      const isRuntimeError = msg.includes("errorDetail") || msg.includes("Cannot read properties") || msg.includes("is not a function") || msg.includes("not a function") || msg.includes("undefined");
      if (isRuntimeError) {
        setFetched(true);
        setRows([]);
        setRawData([]);
      } else {
        ue.error(`Failed to fetch ${key}: ${msg}`);
        setFetched(true);
        setRows([]);
        setRawData([]);
      }
    } finally {
      setLoading(false);
    }
  }
  function handleSelectEntity(key) {
    setSelectedKey(key);
    setSearch("");
    setFetched(false);
    setRows([]);
    setRawData([]);
    fetchEntity(key);
  }
  function copyJson() {
    const text = JSON.stringify(
      rawData,
      (_, v) => typeof v === "bigint" ? v.toString() : v,
      2
    );
    navigator.clipboard.writeText(text).then(() => ue.success("Copied to clipboard"));
  }
  async function handleDelete(id) {
    if (!confirm("Delete this record? This cannot be undone.")) return;
    try {
      if (selectedKey === "cities") await deleteCity.mutateAsync(id);
      else if (selectedKey === "restockOrders")
        await deleteRestockOrder.mutateAsync(id);
      else if (selectedKey === "marketplaceItems")
        await deleteMarketplaceItem.mutateAsync(id);
      else if (selectedKey === "moderationQueue")
        await deleteModerationItem.mutateAsync(id);
      else if (selectedKey === "supportTickets")
        await deleteTicket.mutateAsync(id);
      else if (selectedKey === "recipes") await deleteRecipe.mutateAsync(id);
      else if (selectedKey === "adhocJobs")
        await deleteAdhocJob.mutateAsync(id);
      else if (selectedKey === "healthcareProviders") {
        const actorAny = actor;
        const fn = actorAny.deleteHealthcareProvider;
        if (typeof fn === "function") await fn(id);
      } else if (selectedKey === "tourOperators") {
        const actorAny = actor;
        const fn = actorAny.deleteTourOperator;
        if (typeof fn === "function") await fn(id);
      } else if (selectedKey === "professionalServices") {
        const actorAny = actor;
        const fn = actorAny.deleteProfessionalService;
        if (typeof fn === "function") await fn(id);
      } else if (selectedKey === "healthcareAppointments") {
        const actorAny = actor;
        const fn = actorAny.deleteHealthcareAppointment;
        if (typeof fn === "function") await fn(id);
      } else if (selectedKey === "tourBookings") {
        const actorAny = actor;
        const fn = actorAny.deleteTourBooking;
        if (typeof fn === "function") await fn(id);
      } else if (selectedKey === "serviceBookings") {
        const actorAny = actor;
        const fn = actorAny.deleteServiceBooking;
        if (typeof fn === "function") await fn(id);
      } else if (selectedKey === "donations") {
        const actorAny = actor;
        const fn = actorAny.deleteDonation;
        if (typeof fn === "function") await fn(id);
      } else if (selectedKey === "donationRequests") {
        const actorAny = actor;
        const fn = actorAny.deleteDonationRequest;
        if (typeof fn === "function") await fn(id);
      } else if (selectedKey === "marketSearches") {
        const actorAny = actor;
        const fn = actorAny.deleteMarketSearch;
        if (typeof fn === "function") await fn(id);
      } else if (selectedKey === "communityMembers") {
        const actorAny = actor;
        const fn = actorAny.removeCommunityMember;
        if (typeof fn === "function") await fn(id);
      }
      ue.success("Record deleted");
      fetchEntity(selectedKey);
    } catch (err) {
      ue.error(
        `Delete failed: ${err instanceof Error ? err.message : "Unknown error"}`
      );
    }
  }
  async function handleStatusUpdate(id, status) {
    setStatusSavingId(id);
    try {
      if (selectedKey === "restockOrders") {
        await updateRestockStatus.mutateAsync({
          orderId: id,
          status
        });
      } else if (selectedKey === "moderationQueue") {
        await updateModerationStatus.mutateAsync({
          id,
          status,
          reviewedBy: "admin",
          notes: ""
        });
      } else if (selectedKey === "supportTickets") {
        await updateTicketStatus.mutateAsync({ id, status });
      } else if (selectedKey === "adhocJobs") {
        await updateAdhocJobStatus.mutateAsync({ id, status });
      } else if (selectedKey === "donations") {
        const actorAny = actor;
        const fn = actorAny.updateDonationStatus;
        if (typeof fn === "function") await fn(id, status);
      } else if (selectedKey === "donationRequests") {
        const actorAny = actor;
        const fn = actorAny.updateDonationRequestStatus;
        if (typeof fn === "function") await fn(id, status);
      } else if (selectedKey === "communityParkingBookings") {
        const actorAny = actor;
        const fn = actorAny.updateCommunityParkingBookingStatus;
        if (typeof fn === "function") await fn(id, status);
      } else if (selectedKey === "communityRoomBookings") {
        const actorAny = actor;
        const fn = actorAny.updateCommunityRoomBookingStatus;
        if (typeof fn === "function") await fn(id, status);
      } else if (selectedKey === "communityFoodOrders") {
        const actorAny = actor;
        const fn = actorAny.updateCommunityFoodOrderStatus;
        if (typeof fn === "function") await fn(id, status);
      } else if (selectedKey === "communityWorkOrders") {
        const actorAny = actor;
        const fn = actorAny.updateCommunityWorkOrderStatus;
        if (typeof fn === "function") await fn(id, status);
      }
      fetchEntity(selectedKey);
    } catch (err) {
      ue.error(
        `Update failed: ${err instanceof Error ? err.message : "Unknown error"}`
      );
    } finally {
      setStatusSavingId(null);
    }
  }
  async function handleModalSave(data) {
    setSaving(true);
    try {
      if (selectedKey === "cities") {
        if (modalMode === "edit" && (modalRow == null ? void 0 : modalRow._id)) {
          await updateCity.mutateAsync({
            id: String(modalRow._id),
            name: String(data.name),
            pincodes: String(data.pincodes)
          });
        } else {
          await addCity.mutateAsync({
            name: String(data.name),
            pincode: String(data.pincodes)
          });
        }
      } else if (selectedKey === "cityControls") {
        await saveCityControl.mutateAsync({
          cityId: String(data.cityId),
          moduleName: String(data.moduleName),
          enabled: Boolean(data.enabled)
        });
      } else if (selectedKey === "restockOrders") {
        await createRestockOrder.mutateAsync({
          merchantId: String(data.merchantId),
          supplierName: String(data.supplierName),
          itemName: String(data.itemName),
          quantity: Number(data.quantity),
          notes: String(data.notes ?? "")
        });
      } else if (selectedKey === "marketplaceItems" && modalMode === "add") {
        await createMarketplaceItem.mutateAsync({
          title: String(data.title),
          price: Number(data.price),
          category: String(data.category),
          listingType: String(data.listingType),
          yearOfManufacture: data.yearOfManufacture,
          instagramPhotoLink: "",
          invoiceAvailable: false,
          createdBy: "admin"
        });
      } else if (selectedKey === "marketplaceItems" && modalMode === "edit" && (modalRow == null ? void 0 : modalRow._id)) {
        await updateMarketplaceItem.mutateAsync({
          id: String(modalRow._id),
          title: String(data.title),
          price: Number(data.price),
          status: String(data.status)
        });
      } else if (selectedKey === "moderationQueue") {
        await createModerationItem.mutateAsync({
          contentId: String(data.contentId),
          contentType: String(data.contentType),
          reportedBy: String(data.reportedBy),
          reason: String(data.reason)
        });
      } else if (selectedKey === "recipes") {
        const ingredientLines = String(data.ingredients).split("\n").filter(Boolean);
        const steps = String(data.instructions).split("\n").filter(Boolean);
        await createRecipe.mutateAsync({
          ownerId: "admin",
          title: String(data.title),
          ingredients: ingredientLines.map((l) => ({ name: l, grams: 0 })),
          steps,
          imageLink: "",
          videoLink: "",
          benefits: String(data.category),
          tips: ""
        });
      } else if (selectedKey === "adhocJobs") {
        await createAdhocJob.mutateAsync({
          title: String(data.title),
          description: String(data.description),
          location: String(data.location),
          budget: Number(data.budget),
          category: String(data.category)
        });
      } else if (selectedKey === "healthcareProviders") {
        const actorAny = actor;
        if (modalMode === "edit" && (modalRow == null ? void 0 : modalRow._id)) {
          const fn = actorAny.updateHealthcareProvider;
          if (typeof fn === "function")
            await fn(
              String(modalRow._id),
              String(data.name),
              String(data.specialization),
              String(data.location),
              String(data.phone),
              String(data.availableDays),
              BigInt(Number(data.fee))
            );
        } else {
          const fn = actorAny.addHealthcareProvider;
          if (typeof fn === "function")
            await fn(
              String(data.name),
              String(data.specialization),
              String(data.location),
              String(data.phone),
              String(data.availableDays),
              BigInt(Number(data.fee))
            );
        }
      } else if (selectedKey === "tourOperators") {
        const actorAny = actor;
        if (modalMode === "edit" && (modalRow == null ? void 0 : modalRow._id)) {
          const fn = actorAny.updateTourOperator;
          if (typeof fn === "function")
            await fn(
              String(modalRow._id),
              String(data.name),
              String(data.destination),
              String(data.tourType),
              String(data.duration),
              BigInt(Number(data.price)),
              String(data.phone),
              String(data.description)
            );
        } else {
          const fn = actorAny.addTourOperator;
          if (typeof fn === "function")
            await fn(
              String(data.name),
              String(data.destination),
              String(data.tourType),
              String(data.duration),
              BigInt(Number(data.price)),
              String(data.phone),
              String(data.description)
            );
        }
      } else if (selectedKey === "professionalServices") {
        const actorAny = actor;
        if (modalMode === "edit" && (modalRow == null ? void 0 : modalRow._id)) {
          const fn = actorAny.updateProfessionalService;
          if (typeof fn === "function")
            await fn(
              String(modalRow._id),
              String(data.name),
              String(data.serviceType),
              String(data.location),
              String(data.phone),
              String(data.experience),
              BigInt(Number(data.hourlyRate)),
              String(data.description)
            );
        } else {
          const fn = actorAny.addProfessionalService;
          if (typeof fn === "function")
            await fn(
              String(data.name),
              String(data.serviceType),
              String(data.location),
              String(data.phone),
              String(data.experience),
              BigInt(Number(data.hourlyRate)),
              String(data.description)
            );
        }
      } else if (selectedKey === "donations") {
        const actorAny = actor;
        const fn = actorAny.addDonation;
        if (typeof fn === "function")
          await fn(
            String(data.category),
            String(data.description),
            String(data.quantity),
            String(data.location),
            String(data.donorPhone),
            String(data.donorPhone),
            String(data.donorName),
            "dashboard"
          );
      } else if (selectedKey === "donationRequests") {
        const actorAny = actor;
        const fn = actorAny.requestDonation;
        if (typeof fn === "function")
          await fn(
            String(data.category),
            String(data.description),
            String(data.quantityNeeded),
            String(data.location),
            String(data.requesterPhone),
            String(data.requesterName),
            "dashboard"
          );
      } else if (selectedKey === "communityParkingBookings") {
        const actorAny = actor;
        const fn = actorAny.createCommunityParkingBooking;
        if (typeof fn === "function")
          await fn({
            memberPhone: String(data.memberPhone),
            communityId: String(data.communityId),
            parkingType: String(data.parkingType ?? "open"),
            startDate: String(data.startDate),
            endDate: String(data.endDate),
            cost: Number(data.cost ?? 0),
            status: "pending"
          });
      } else if (selectedKey === "communityRoomBookings") {
        const actorAny = actor;
        const fn = actorAny.createCommunityRoomBooking;
        if (typeof fn === "function")
          await fn({
            memberPhone: String(data.memberPhone),
            communityId: String(data.communityId),
            facilityType: String(data.facilityType),
            bookingDate: String(data.bookingDate),
            timeSlot: String(data.timeSlot),
            description: String(data.description ?? ""),
            status: "pending"
          });
      } else if (selectedKey === "communityFoodOrders") {
        const actorAny = actor;
        const fn = actorAny.createCommunityFoodOrder;
        if (typeof fn === "function")
          await fn({
            buyerPhone: String(data.buyerPhone),
            sellerPhone: String(data.sellerPhone),
            communityId: String(data.communityId),
            mealDescription: String(data.mealDescription),
            cuisineType: String(data.cuisineType ?? "Indian"),
            dietary: String(data.dietary ?? "none"),
            deliveryTime: String(data.deliveryTime ?? ""),
            quantity: Number(data.quantity ?? 1),
            cost: Number(data.cost ?? 0),
            status: "pending"
          });
      } else if (selectedKey === "communityWorkOrders") {
        const actorAny = actor;
        const fn = actorAny.createCommunityWorkOrder;
        if (typeof fn === "function")
          await fn({
            memberPhone: String(data.memberPhone),
            communityId: String(data.communityId),
            serviceType: String(data.serviceType),
            description: String(data.description),
            scheduledDate: String(data.scheduledDate ?? ""),
            status: "pending"
          });
      }
      setModalRow(null);
      fetchEntity(selectedKey);
    } catch (err) {
      ue.error(
        `Save failed: ${err instanceof Error ? err.message : "Unknown error"}`
      );
    } finally {
      setSaving(false);
    }
  }
  const SOURCE_TABLES = [
    "users",
    "orders",
    "adhocJobs",
    "marketplaceItems",
    "supportTickets",
    "restockOrders",
    "donations",
    "donationRequests"
  ];
  const showSourceFilter = SOURCE_TABLES.includes(selectedKey);
  const filtered = rows.filter((r) => {
    const matchSearch = !search || Object.values(r).some(
      (v) => String(v).toLowerCase().includes(search.toLowerCase())
    );
    const matchSource = !showSourceFilter || sourceFilter === "All Sources" || String(r.source ?? "").toLowerCase() === sourceFilter.toLowerCase();
    return matchSearch && matchSource;
  });
  const displayCols = entity.columns;
  const actionCols = entity.canEdit || entity.canDelete || entity.canStatusUpdate;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "flex h-[calc(100vh-64px)] overflow-hidden",
      "data-ocid": "data_explorer.page",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: "w-52 shrink-0 border-r border-border bg-card overflow-y-auto flex flex-col", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "px-3 py-3 border-b border-border flex-shrink-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Database, { className: "w-4 h-4 text-primary" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold text-sm text-foreground", children: "Data Explorer" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] text-muted-foreground mt-0.5", children: "Live backend query viewer" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("nav", { className: "py-2 px-2 flex-1 overflow-y-auto", children: [...ENTITIES, ...LANGUAGE_LEARNING_ENTITIES].map((ent) => {
            const Icon = ent.icon;
            const isActive = selectedKey === ent.key;
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "button",
              {
                type: "button",
                onClick: () => handleSelectEntity(ent.key),
                "data-ocid": `data_explorer.entity.${ent.key}`,
                className: [
                  "w-full flex items-center gap-2.5 px-2.5 py-2 rounded-md text-sm transition-all text-left mb-0.5",
                  isActive ? "bg-primary text-primary-foreground font-medium" : "text-muted-foreground hover:bg-muted hover:text-foreground"
                ].join(" "),
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "w-3.5 h-3.5 shrink-0" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "truncate", children: ent.label })
                ]
              },
              ent.key
            );
          }) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("main", { className: "flex-1 min-w-0 flex flex-col bg-background overflow-hidden", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 px-4 py-3 border-b border-border bg-card flex-shrink-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 flex-1 min-w-0", children: [
              (() => {
                const Icon = entity.icon;
                return /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "w-4 h-4 text-primary shrink-0" });
              })(),
              /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "font-semibold text-sm text-foreground", children: entity.label }),
              fetched && /* @__PURE__ */ jsxRuntimeExports.jsxs(Badge, { variant: "secondary", className: "text-xs tabular-nums", children: [
                Math.max(0, filtered.length),
                " record",
                filtered.length !== 1 ? "s" : ""
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 flex-shrink-0", children: [
              entity.canAdd && fetched && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                Button,
                {
                  size: "sm",
                  className: "h-8 gap-1.5 text-xs",
                  onClick: () => {
                    setModalMode("add");
                    setModalRow(null);
                  },
                  "data-ocid": "data_explorer.add_button",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "w-3.5 h-3.5" }),
                    "Add New"
                  ]
                }
              ),
              showSourceFilter && fetched && /* @__PURE__ */ jsxRuntimeExports.jsx(
                "select",
                {
                  value: sourceFilter,
                  onChange: (e) => setSourceFilter(e.target.value),
                  className: "h-8 px-2 rounded border border-border bg-muted/30 text-xs text-foreground",
                  "data-ocid": "data_explorer.source_filter",
                  children: ALL_SOURCES.map((s) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: s, children: s }, s))
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "absolute left-2.5 top-1/2 -translate-y-1/2 w-3.5 h-3.5 text-muted-foreground pointer-events-none" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  Input,
                  {
                    placeholder: "Filter records…",
                    value: search,
                    onChange: (e) => setSearch(e.target.value),
                    className: "pl-8 h-8 text-xs w-52",
                    "data-ocid": "data_explorer.search_input"
                  }
                )
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                Button,
                {
                  variant: "outline",
                  size: "sm",
                  className: "h-8 gap-1.5 text-xs",
                  onClick: () => fetchEntity(selectedKey),
                  disabled: loading,
                  "data-ocid": "data_explorer.refresh_button",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      RefreshCw,
                      {
                        className: `w-3.5 h-3.5 ${loading ? "animate-spin" : ""}`
                      }
                    ),
                    loading ? "Loading…" : "Refresh"
                  ]
                }
              ),
              activeTab === "json" && fetched && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                Button,
                {
                  variant: "outline",
                  size: "sm",
                  className: "h-8 gap-1.5 text-xs",
                  onClick: copyJson,
                  "data-ocid": "data_explorer.copy_json_button",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(ClipboardCopy, { className: "w-3.5 h-3.5" }),
                    "Copy JSON"
                  ]
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Tabs,
            {
              value: activeTab,
              onValueChange: (v) => setActiveTab(v),
              className: "flex-1 min-h-0 flex flex-col overflow-hidden",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs(TabsList, { className: "h-9 rounded-none border-b border-border bg-muted/30 shrink-0 w-full justify-start px-4 gap-1", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    TabsTrigger,
                    {
                      value: "table",
                      className: "text-xs h-7",
                      "data-ocid": "data_explorer.table_tab",
                      children: "Table View"
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    TabsTrigger,
                    {
                      value: "json",
                      className: "text-xs h-7",
                      "data-ocid": "data_explorer.json_tab",
                      children: "Raw JSON"
                    }
                  )
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  TabsContent,
                  {
                    value: "table",
                    className: "flex-1 min-h-0 m-0 overflow-auto",
                    children: !fetched && !loading ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "div",
                      {
                        className: "flex flex-col items-center justify-center h-full gap-3 text-center py-20",
                        "data-ocid": "data_explorer.empty_state",
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(Database, { className: "w-12 h-12 text-muted-foreground/30" }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm font-medium", children: "Select an entity and click Load to explore backend data" }),
                          /* @__PURE__ */ jsxRuntimeExports.jsxs(
                            Button,
                            {
                              size: "sm",
                              onClick: () => fetchEntity(selectedKey),
                              "data-ocid": "data_explorer.load_button",
                              children: [
                                "Load ",
                                entity.label
                              ]
                            }
                          )
                        ]
                      }
                    ) : loading ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-4 space-y-2", children: Array.from({ length: 8 }).map((_, i) => (
                      // biome-ignore lint/suspicious/noArrayIndexKey: skeleton
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-10 w-full rounded" }, i)
                    )) }) : filtered.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "div",
                      {
                        className: "flex flex-col items-center justify-center h-full gap-2 py-16",
                        "data-ocid": "data_explorer.no_results_state",
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm font-medium", children: search ? `No records matching "${search}"` : `No ${entity.label.toLowerCase()} yet` }),
                          !search && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-xs", children: entity.canAdd ? "Use the Add button to create the first record, or load sample data from the dashboard." : "Records will appear here once data is created via chatbot or dashboard." }),
                          search && /* @__PURE__ */ jsxRuntimeExports.jsx(
                            Button,
                            {
                              variant: "ghost",
                              size: "sm",
                              onClick: () => setSearch(""),
                              children: "Clear search"
                            }
                          ),
                          entity.canAdd && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                            Button,
                            {
                              size: "sm",
                              onClick: () => {
                                setModalMode("add");
                                setModalRow(null);
                              },
                              children: [
                                /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "w-3.5 h-3.5 mr-1.5" }),
                                "Add First ",
                                entity.label.replace(/s$/, "")
                              ]
                            }
                          )
                        ]
                      }
                    ) : /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "w-full text-xs", "data-ocid": "data_explorer.table", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { className: "sticky top-0 bg-muted/60 border-b border-border z-10", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
                        displayCols.map((col) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                          "th",
                          {
                            className: "text-left py-2.5 px-3 font-semibold text-muted-foreground uppercase tracking-wider text-[10px] whitespace-nowrap",
                            children: col
                          },
                          col
                        )),
                        showSourceFilter && /* @__PURE__ */ jsxRuntimeExports.jsx("th", { className: "text-left py-2.5 px-3 font-semibold text-muted-foreground uppercase tracking-wider text-[10px] whitespace-nowrap", children: "Source" }),
                        actionCols && /* @__PURE__ */ jsxRuntimeExports.jsx("th", { className: "text-left py-2.5 px-3 font-semibold text-muted-foreground uppercase tracking-wider text-[10px] whitespace-nowrap", children: "Actions" })
                      ] }) }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: filtered.map((row, idx) => {
                        const rowId = String(row._id ?? "");
                        const rowStatus = String(row._status ?? "");
                        return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                          "tr",
                          {
                            tabIndex: selectedKey === "marketSearches" ? 0 : void 0,
                            role: selectedKey === "marketSearches" ? "button" : void 0,
                            className: `border-b border-border/40 hover:bg-muted/20 transition-colors${selectedKey === "marketSearches" ? " cursor-pointer" : ""}`,
                            "data-ocid": `data_explorer.row.${idx + 1}`,
                            onClick: selectedKey === "marketSearches" ? () => setMarketSearchDetailRow(row) : void 0,
                            onKeyDown: selectedKey === "marketSearches" ? (e) => e.key === "Enter" && setMarketSearchDetailRow(row) : void 0,
                            children: [
                              displayCols.map((col) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                                "td",
                                {
                                  className: "py-2 px-3 font-mono text-foreground max-w-[200px] truncate whitespace-nowrap",
                                  title: String(row[col] ?? ""),
                                  children: String(row[col] ?? "—")
                                },
                                col
                              )),
                              showSourceFilter && /* @__PURE__ */ jsxRuntimeExports.jsx("td", { className: "py-2 px-3 whitespace-nowrap", children: /* @__PURE__ */ jsxRuntimeExports.jsx(SourceBadge, { source: String(row.source ?? "") }) }),
                              actionCols && /* @__PURE__ */ jsxRuntimeExports.jsx("td", { className: "py-2 px-3 whitespace-nowrap", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5", children: [
                                entity.canStatusUpdate && entity.statusOptions && /* @__PURE__ */ jsxRuntimeExports.jsx(
                                  InlineStatusSelect,
                                  {
                                    currentStatus: rowStatus,
                                    options: entity.statusOptions,
                                    onSave: (s) => handleStatusUpdate(rowId, s),
                                    saving: statusSavingId === rowId
                                  }
                                ),
                                entity.canEdit && /* @__PURE__ */ jsxRuntimeExports.jsx(
                                  Button,
                                  {
                                    size: "sm",
                                    variant: "ghost",
                                    className: "h-6 px-1.5",
                                    onClick: (e) => {
                                      e.stopPropagation();
                                      setModalMode("edit");
                                      setModalRow(row);
                                    },
                                    "data-ocid": `data_explorer.edit_button.${idx + 1}`,
                                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(Pen, { className: "w-3 h-3" })
                                  }
                                ),
                                selectedKey === "marketSearches" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                                  Button,
                                  {
                                    size: "sm",
                                    variant: "ghost",
                                    className: "h-6 px-1.5 text-primary hover:text-primary",
                                    onClick: (e) => {
                                      e.stopPropagation();
                                      setMarketSearchDetailRow(row);
                                    },
                                    "data-ocid": `data_explorer.view_button.${idx + 1}`,
                                    title: "View search results & recommendations",
                                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "w-3 h-3" })
                                  }
                                ),
                                entity.canDelete && /* @__PURE__ */ jsxRuntimeExports.jsx(
                                  Button,
                                  {
                                    size: "sm",
                                    variant: "ghost",
                                    className: "h-6 px-1.5 text-destructive hover:text-destructive",
                                    onClick: (e) => {
                                      e.stopPropagation();
                                      handleDelete(rowId);
                                    },
                                    "data-ocid": `data_explorer.delete_button.${idx + 1}`,
                                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(Trash2, { className: "w-3 h-3" })
                                  }
                                )
                              ] }) })
                            ]
                          },
                          idx
                        );
                      }) })
                    ] })
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  TabsContent,
                  {
                    value: "json",
                    className: "flex-1 min-h-0 m-0 overflow-hidden",
                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "h-full", children: !fetched ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center justify-center h-40", children: /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm", children: "Load data first using the Refresh button" }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "pre",
                      {
                        className: "p-4 text-xs font-mono text-foreground leading-relaxed whitespace-pre-wrap break-all",
                        "data-ocid": "data_explorer.json_output",
                        children: JSON.stringify(
                          rawData,
                          (_, v) => typeof v === "bigint" ? v.toString() : v,
                          2
                        )
                      }
                    ) })
                  }
                )
              ]
            }
          )
        ] }),
        modalMode !== null && /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Modal,
          {
            title: `${modalMode === "add" ? "Add New" : "Edit"} ${entity.label.replace(/s$/, "")}`,
            onClose: () => {
              setModalMode(null);
              setModalRow(null);
            },
            children: [
              selectedKey === "cities" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                CityForm,
                {
                  initial: modalRow ?? void 0,
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "cityControls" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                CityControlForm,
                {
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "restockOrders" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                RestockOrderForm,
                {
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "marketplaceItems" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                MarketplaceItemForm,
                {
                  initial: modalRow ?? void 0,
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "moderationQueue" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                ModerationFlagForm,
                {
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "recipes" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(RecipeForm, { onSave: (d) => handleModalSave(d), saving }),
              selectedKey === "adhocJobs" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(AdhocJobForm, { onSave: (d) => handleModalSave(d), saving }),
              selectedKey === "healthcareProviders" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                HealthcareProviderForm,
                {
                  initial: modalRow ?? void 0,
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "tourOperators" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                TourOperatorForm,
                {
                  initial: modalRow ?? void 0,
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "professionalServices" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                ProfessionalServiceForm,
                {
                  initial: modalRow ?? void 0,
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "donations" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(DonationForm, { onSave: (d) => handleModalSave(d), saving }),
              selectedKey === "donationRequests" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                DonationRequestForm,
                {
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "communityParkingBookings" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                CommunityParkingForm,
                {
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "communityRoomBookings" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                CommunityRoomForm,
                {
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "communityFoodOrders" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                CommunityFoodOrderForm,
                {
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              ),
              selectedKey === "communityWorkOrders" && modalMode === "add" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                CommunityWorkOrderForm,
                {
                  onSave: (d) => handleModalSave(d),
                  saving
                }
              )
            ]
          }
        ),
        marketSearchDetailRow !== null && /* @__PURE__ */ jsxRuntimeExports.jsx(
          MarketSearchDetailModal,
          {
            row: marketSearchDetailRow,
            onClose: () => setMarketSearchDetailRow(null)
          }
        )
      ]
    }
  );
}
export {
  DataExplorerPage as default
};
